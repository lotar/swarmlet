{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The splash page in the main window needs no IPC; core defaults only. The node UI has no general native IPC; a separate macOS capability allows window dragging only.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
