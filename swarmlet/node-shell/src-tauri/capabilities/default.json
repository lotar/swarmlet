{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The splash page in the main window needs no IPC; core defaults only. The node UI at http://127.0.0.1:47800 is a remote origin and gets no Tauri IPC at all.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
