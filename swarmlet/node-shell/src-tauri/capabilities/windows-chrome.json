{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "windows-chrome",
  "description": "The Windows node UI draws its own caption: it can drag, minimize, maximize and close (hide) its frameless window. No other native command is exposed to the localhost UI.",
  "platforms": ["windows"],
  "windows": ["main"],
  "local": true,
  "remote": { "urls": ["http://127.0.0.1:47800/*"] },
  "permissions": [
    "core:window:allow-start-dragging",
    "core:window:allow-internal-toggle-maximize",
    "core:window:allow-minimize",
    "core:window:allow-close"
  ]
}
