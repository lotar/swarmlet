# L1 — local-llm rig scripts (`/Users/lotar/projects/local-llm`)

Audit window 2026-09-16 10:35–11:11 +0200. Repo HEAD `d10af40`. 114 tracked files, 25 `bin/*.sh`,
`models/` 267 GB, `logs/` 46 MB / 105 entries.

**The repo changed under me mid-audit.** Commit `d10af40` ("llm-switch: never reap the mesh
engine (or any other project's server)") landed 2026-09-16 **10:57:46**, growing
`bin/llm-switch.sh` 20306 → 21334 bytes. Every finding below is timestamp-qualified.

**Commands I ran that touch launchctl:** `bin/llm-switch.sh status` (3×) only, which internally
runs the read-only query `launchctl print gui/501/<label>`. No mutating launchd verb, no
bootstrap/bootout/enable/disable, no plist write, no sudo, no git mutation, no repo edit.

---

## 1. E2E CHECK

### 1a. Briefed commands — literal output

| # | command | observed (trimmed) | verdict |
|---|---|---|---|
| 1 | `for f in bin/*.sh; do bash -n "$f" \|\| echo "SYNTAX FAIL $f"; done` | no output; 25 files scanned | **works** — 25/25 parse |
| 2 | `bin/llm-switch.sh list` | `usage: llm-switch gemma4\|v4flash\|flashnext\|off\|status`, `rc=2` | **broken** — documented in CLAUDE.md:54,82-84; verb does not exist |
| 3 | `bin/llm-switch.sh status` (10:38) | 3 rows all `not loaded` / `port … down`; **`ORPHAN pid=80707 port=8100 cmd=/Users/lotar/.swarmlet/releases/2026091225-…/engine/llama-server -m /Users/lotar/.swarmlet/models/Qwen3.8-27B-Q8_0.gguf …`**; `18 UNKNOWN-LISTENER port(s) not shown` | **works — and this was a live hazard** (see §1c) |
| 4 | `LLM_SWITCH_VERBOSE=1 bin/llm-switch.sh status` (11:00, 11:01 ×3) | 18 UNKNOWN-LISTENER lines, **8100 absent** | **flaky-by-time**: guard landed at 10:57:26 between runs 3 and 4 |
| 5 | `bin/cleanup.sh` (dry run) | `== 1 … ==` `== 2 … ==` (0 candidates); `would truncate logs/v4flash.err.log (17M)`, `would truncate logs/flashnext.err.log (17M)`, `dry-run only` | **works**, but see D6 — the documented serving-log skip is dead code |
| 6 | `bin/llm-tune doctor` | `NOT READY`; `BLOCK swap free 1G < floor 4G`; `BLOCK … load 21.57 across 18 cores = 1.20/core > 0.75`; `fit: NEED 133.7 GiB … HAVE metal-free 107.3 GiB … preflight-fit: DOES NOT FIT`; `fit basis ctx=262144 kv=f16 cache-ram=4096GiB`; `candidate lint: clean` | **works** (gate fires); one label bug S2 |
| 7 | `bin/llm-tune plan` | `20260916-1039 … matrix bin/tune-matrix.env`, 15 candidate rows, `lint: clean` | **works** |
| 8 | `PRINT=1 bin/serve-flashnext.sh` | full 30-flag command line ending `--mmproj …/mmproj-F16.gguf --image-min-tokens 1024`, `rc=0`, no load | **works** |
| 9 | `PRINT=1 bin/serve-gemma4.sh`; `PRINT=1 bin/serve-v4flash.sh` | `/bin/bash: bin/serve-gemma4.sh: No such file or directory`, `rc=127` (same for v4flash) | **n/a — no engine touched**, disclosed in §8 |
| 10 | `bin/llm-lanes` | 4 rows `8099/8095/8098/8089 DOWN` | **works** |
| 11 | `bin/bench-model.sh` / `verify-model.sh` / `probe-diff.sh` / `verify-agentic.sh` / `preflight-fit.sh` with 0 args | each prints its `usage:` line, `rc=0`, no network | **works** (5/5 argument-validation paths live) |
| 12 | `ls -la models/`; `du -sh logs/`; `ls -S logs/ \| head` | 6 model dirs / 267 GB; `46M logs/`; top 3 = `v4flash.err.log` 17M, `flashnext.err.log` 17M, `v4-triage-full-label-server.err.log` 3.0M | **works** |
| 13 | `command -v shellcheck && shellcheck -S warning bin/*.sh` | `shellcheck NOT INSTALLED` | **could not test** |
| 14 | `bin/llm-switch-test.sh` | not run | **could not test** — see §8 |

### 1b. Ground-truth cross-check (registry vs scripts vs disk vs docs)

| surface | value | evidence |
|---|---|---|
| registry | `NAMES="gemma4 v4flash flashnext"` | `grep -n '^NAMES=' bin/llm-switch.sh` → `52:NAMES="gemma4 v4flash flashnext"` |
| ports | 8089 / 8098 / 8099 | `bin/llm-switch.sh:60-66` |
| `bin/serve-*.sh` | 6: `flashnext`, `flashnext-test`, `gemma`, `qwen38`, `v4`, `aeon-test` | `ls bin/serve-*.sh` |
| launchd plists | 4: `flashnext`, `gemma4`, `v4flash`, **`qwen38`** | `ls ~/Library/LaunchAgents/com.lotar.llm-*.plist` |
| enable bits | `com.lotar.llm-flashnext => enabled`; 16 other `com.lotar.llm-*` disabled | `launchctl print-disabled gui/501` |
| `models/` dirs | 6 | `ls -d models/*/` |

| # | mismatch | command that shows it |
|---|---|---|
| M1 | CLAUDE.md:54+82-84 document `llm-switch list` ("shows on-disk size so a partial download is visible"). No `list` verb; `rc=2`. | `bin/llm-switch.sh list` |
| M2 | CLAUDE.md:41,138 say "add a `MODELS` row in `bin/llm-switch.sh`". No `MODELS` symbol exists; it is `NAMES` (line 52). 2 doc refs to a symbol that never existed. | `grep -n MODELS bin/llm-switch.sh` → 0 matches |
| M3 | **`bin/serve-qwen38.sh` (6488 B, untracked) + `com.lotar.llm-qwen38.plist` + `models/qwen3.8-27b/` (30 GB, 3 SHA-pinned files, all present) — a 4th complete, runnable lane that is in no registry.** | `ls bin/serve-qwen38.sh ~/Library/LaunchAgents/com.lotar.llm-qwen38.plist; ls -la models/qwen3.8-27b/` |
| M4 | `models/` dirs no script or doc points at: `qwen3.5-draft` 9.1 G, `qwen3.6-35b-a3b` 29 G, `qwen3.8-flash-next-hf` 22 M = **38.1 GB**. | `grep -rIl "models/qwen3.5-draft" .` → NONE (same for the other two) |
| M5 | `bin/llm-switch-reconcile.sh:7` — "RunAtLoad+KeepAlive is set on all **ten** com.lotar.llm-* plists". 4 exist. | `ls ~/Library/LaunchAgents/com.lotar.llm-*.plist \| wc -l` → 4 |
| M6 | `bin/serve-aeon-test.sh:10` `DIR=…/models/qwen3.8-27b-aeon` — dir does not exist. | `ls models/` (no `qwen3.8-27b-aeon`) |
| M7 | `bin/llm-switch.sh:9` header: flashnext is "128K ctx … IQ1_S". On disk: `CTX=262144`, `UD-Q4_K_XL`. | `grep -n 'CTX=\|MODEL=' bin/serve-flashnext.sh` |
| M8 | README:369 "predict 32768" vs actual `--predict 52428`. | `bin/llm-tune doctor` baseline line |
| M9 | README:517 says llm-switch "only owns gemma4/v4flash/flashnext/ds4vision" and co-tenants are :8112/:8116/ollama. `IGNORE_PORTS` now also carries 8100; ds4vision is gone. | `grep -n IGNORE_PORTS bin/llm-switch.sh` → `8100 8111 8112 8114 8115 8116 11434` |

### 1c. Live hazard observed (now fixed at HEAD)

At **10:38** `status` classified the Swarmlet mesh engine as `ORPHAN`:

```
ORPHAN pid=80707 port=8100 cmd=/Users/lotar/.swarmlet/releases/2026091225-6ed98df4-…/engine/llama-server -m /Users/lotar/.swarmlet/models/Qwen3.8-27B-Q8_0.gguf --port 8100 …
```

`is_llama_cmd()` (then) guarded only `*/lib/ollama/*`, `*/Cellar/ollama/*`, `*/ollama/bin/ollama`.
`reap_orphans()` (`llm-switch.sh:372,384`) runs `kill -TERM` then `kill -KILL` on every ORPHAN, so
`llm-switch off` or **any** `llm-switch <lane>` would have terminated the engine serving other
people's sessions. Probe of the function at the time:

```
$ source bin/llm-switch.sh; is_llama_cmd "$(ps -o command= -p 35099)"; echo rc=$?
rc=0        # 0 = "ours, reapable"
```

Commit `d10af40` (10:57:46) added `*/.swarmlet/*|*swarmlet/releases/*|*/projects/ai-fleet/*`
return 1 (`bin/llm-switch.sh:107`) and `8100` to `IGNORE_PORTS` (line 57). Re-probe at 11:01:
`is_llama_cmd rc=1`, and `scan_unaccounted | grep -c 8100` → `0`. **Closed.**
The mesh engine pid itself moved 80707 → 35099 → 30373 within 35 min (11:11 reading) — that is the
mesh lane's problem, not this one; see §7.

---

## 2. REQUIREMENTS

| # | requirement (explicit or implied) | owner | keep/drop + what breaks if dropped |
|---|---|---|---|
| R1 | Exactly one big model resident; `llm-switch` enforces it | **UNOWNED** — no named human; the only client left, hermes, points at 6 dead ports | **KEEP the kill-safety half, DROP the "prod lane" half.** README:372 already retired :8099 (2026-09-15) in favour of the mesh on :47800. Nothing breaks: no live listener exists on 8089/8090/8095/8098/8099 (`for p in 8089 8090 8095 8098 8099; do lsof -nP -iTCP:$p -sTCP:LISTEN; done` → all empty). The double-load guard must stay because the mesh engine occupies the same RAM. |
| R2 | Registry has 3 rows, one per model lane | **UNOWNED** | **DROP 2 of 3.** `serve-gemma.sh` exits 66 (model absent) and `serve-v4.sh` exits 65/66 (model absent, SHA-pinned path outside the repo). Both plists are `disabled`. 2 of 3 registry rows describe lanes that cannot start. Nothing breaks — the failure is already at the model check, before any load. |
| R3 | Keep the gemma4 lane because the GGUF is re-downloadable | **UNOWNED** ("Lane kept" in serve-gemma.sh header) | **DROP.** The re-fetch is one line already written in the script header. A one-line comment in README replaces 3437 B + a plist + 3 registry arms. |
| R4 | Keep the v4flash lane | **UNOWNED** ("model absent … NOT re-downloadable", `llm-switch.sh:20-21`) | **DROP.** 8077 B `serve-v4.sh` + 199 B `set-mem-v4.sh` + plist + 3 registry arms for a lane whose artifact is explicitly gone forever. |
| R5 | Tuning must prove a hardware budget before measuring | **UNOWNED** (`CLAUDE.md:6`) | **KEEP.** It is the only gate in this repo that has ever fired — `logs/tune/ledger.tsv` has exactly 1 row, verdict `VOID-BUDGET`. |
| R6 | Tuning owns :8095, never :8099 | **UNOWNED** (`CLAUDE.md:6`) | **KEEP.** Cheap, and the failure it prevents (a second client moving a logit) is invisible otherwise. |
| R7 | 15-candidate matrix × 9 accuracy probes | **UNOWNED** | **DROP 12 of 15 rows.** 12 days of harness, 1 recorded trial, 246-byte ledger. The matrix's own header says "the first rows must be the ones worth running if you only get three" — the other 12 are untested speculation. |
| R8 | `cleanup.sh` skips the log of a serving model | **UNOWNED** — the claim is in `CLAUDE.md:130-131` | **DROP the claim (3 lines to implement, 0 to delete).** `$PORTS` is computed at `cleanup.sh:54` and read nowhere: `grep -c PORTS bin/cleanup.sh` → `1`. The safety property documented does not exist. |
| R9 | `logs/` gets rotated | **owner = the fleet lane** (`com.fleet.llm-log-rotate` launchd, 04:10 daily) — not this repo | **DROP one of two rotators.** The fleet rotator triggers at 20 MB and has produced 0 archives (`ls logs/*.log.gz \| wc -l` → 0) while the biggest log is 17 MB; it has never fired. `cleanup.sh` triggers at 5 MB and does fire. |
| R10 | `sync_hermes` reports drift, never rewrites | **UNOWNED** | **KEEP.** It is the only thing that would surface `~/.hermes/config.yaml` carrying 6 local providers on 8088/8091/8093/8094/8097/8099 — all six ports have no listener. |
| R11 | `preflight-fit.sh` gates loads | **UNOWNED** | **KEEP** — `tune-rig.env:16` (`RIG_PREFLIGHT`) is a live caller. |
| R12 | Probe fixtures live in `bin/tune-probes.json` | **UNOWNED** | **KEEP the file, DROP the 2 duplicated fixtures.** |

---

## 3. DELETED

Total Class-A reclaim: **88 KB tracked scripts + 60 KB `__pycache__` + 46 MB logs (94/105 entries
unreferenced)**. `models/` is untouched by every proposal below.

| # | class | path(s) | size | proof nothing references it | risk if wrong | restore |
|---|---|---|---|---|---|---|
| D1 | **A** | `bin/llm-switch-test.sh.bak-flashnext` | 38 199 B | `shasum -a 256` → **identical** to `bin/llm-switch-test.sh` (`1cbd5ee59a21…09cdd`); `diff \| wc -l` → `0`. A tracked byte-for-byte duplicate of its neighbour. | none — same bytes | `git show HEAD:bin/llm-switch-test.sh.bak-flashnext > …` |
| D2 | **A** | `bin/__pycache__/` (4 `.pyc`) | 60 KB | gitignored (`.gitignore:24`); regenerated on import | none | automatic |
| D3 | **A** | `bin/v4-guard.sh` | 2 268 B | 0 refs repo-wide: `grep -rIl v4-guard.sh .` (excl. .git/models/logs) → NONE. A **killer** with no owner: `pkill -f "deepseek-v4-flash"`, `pkill -f "DeepSeek-V4"` (lines 20-21), and it calls `bin/v4-test-window.sh`, which does not exist. | low — nothing invokes it; leaving it is the bigger hazard (a pattern kill one `bash` away) | `git show HEAD:bin/v4-guard.sh` |
| D4 | **A** | `bin/bench-v4-triage.py` | 17 191 B | 0 refs repo-wide; the V4 lane it triaged is gone | low | `git show HEAD:…` |
| D5 | **A** | `bin/test-serve-v4-deployment.sh` | 2 149 B | only ref is `.hermes/plans/2026-08-02…-uncensoring.md` (a plan, not a caller) | low | `git show HEAD:…` |
| D6 | **A** | `bin/flashnext-rewire.sh` + `bin/flashnext-swap.sh` | 2 636 + 1 136 B | 0 refs repo-wide. `rewire` is a one-shot `sed` rewriter already applied (`-e 's/qwen38/flashnext/g'`, line 22); `swap` is the qwen38→flashnext cutover, already done (line 2). Re-running either would corrupt the current registry. | none — running them is now unsafe | `git show HEAD:…` |
| D7 | **B** | `bin/serve-gemma.sh` + `com.lotar.llm-gemma4.plist` + NAMES/port_of/model_of/ctx_of arms + README §Gemma | 3 437 B code, 32.6 GB already absent | model absent → the script self-exits 66; plist `disabled` | re-fetch is 1 documented line | `git show HEAD:bin/serve-gemma.sh`; `bin/fetch-model.sh mradermacher/gemma-4-31B-it-heretic-GGUF models gemma-4-31B-it-heretic.Q8_0.gguf` |
| D8 | **B** | `bin/serve-v4.sh` + `bin/set-mem-v4.sh` + plist + 3 registry arms + `logs/v4flash.{out,err}.log` | 8 077 + 199 B code, 17 MB log | model NOT re-downloadable (`llm-switch.sh:20-21`); plist `disabled` | irreversible lane loss — **owner must confirm before deleting** | `git show HEAD:…` (code only; weights are gone) |
| D9 | **B** | `bin/serve-aeon-test.sh` | 1 524 B | only ref is `plans/eval100-plan-20260902.md`; points at `models/qwen3.8-27b-aeon`, which does not exist; binds :8095, already owned by `serve-flashnext-test.sh` | a second :8095 lane is a sole-residency hazard, not an asset | `git show HEAD:…` |
| D10 | **B** | `run-dev.sh` (1 165 B) + `run-dev-64k.sh` (1 264 B) | 2 429 B | 0 refs; `run-dev-glm.sh` has 1 (README) | these are root SWE-bench harness files CLAUDE.md:34-42 protects as a class — confirm the eval owner signs off | `git show HEAD:…` |
| D11 | **B** | 94 of 105 entries in `logs/` | 46 MB total | `for f in logs/*; do grep -rIq "$(basename $f)" …; done` → 11 referenced, **94 unreferenced** | these are measurement history (`logs/measured-results.tsv` has 9 refs and must stay) | uncommitted-but-gitignored; keep a tarball first |
| D12 | **B** | 12 of 15 rows in `bin/tune-matrix.env` | ~6 KB | `logs/tune/ledger.tsv` = 1 row, `VOID-BUDGET` | none measurable; the 3 cheapest rows are in the first 3 positions already | `git show HEAD:bin/tune-matrix.env` (untracked → keep a copy) |

**LEAVE ALONE (adjacent lane):** `bin/h3-video.sh`, `bin/music3-gen.py`,
`bin/music3-batch-punk.py` are 0-ref and unrelated to LLM serving, but root `h3.c/`,
`h3-outputs/` and `music3-venv/` suggest a sibling auditor's lane. Handoff, not a proposal.

**The ~10 % I expect to come back:** (a) one of the two log rotators — collapsing to one will
re-expose the case the other covered; (b) `bin/serve-v4.sh` **if** the 86.7 GB artifact ever
reappears (it is the only SHA-pinned copy of that config); (c) `bin/verify-agentic.sh` +
`bin/probe-diff.sh` — the only *discriminating* agent-loop probes, which D11 would archive under
`logs/` housekeeping if not explicitly exempted; (d) `bin/llm-switch-test.sh` itself, once someone
is allowed to run it.

**Class C — do not touch:** `bin/llm-switch.sh`, `bin/llm-switch-reconcile.sh`, `bin/llm-lanes`,
`bin/llm-tune` + `bin/tune-{rig,matrix,probes}`, `bin/serve-flashnext.sh`, `bin/cleanup.sh`,
`bin/preflight-fit.sh`, `bin/verify-model.sh`, `bin/gguf-meta.py`, `bin/llm-switch-test.sh` (+ its
4 stubs), `bin/serve-qwen38.sh` + its plist + `models/qwen3.8-27b/`.

---

## 4. SIMPLIFIED

Survivors only.

| # | change | why |
|---|---|---|
| S1 | `llm-switch`'s four parallel `case` tables (`port_of`/`model_of`/`ctx_of`/`label`) + `NAMES` → one `LANES` row per lane. | There are 4 places to edit to add a lane and `serve-qwen38.sh` proves the failure: a complete 4th lane exists that `sys` doesn't know about (M3). |
| S2 | `bin/llm-lanes:9` `PORTS="8099 8095 8098 8089"` — a second hardcoded port list to hand-sync with `port_of()`. Derive it (`llm-switch-reconcile.sh:26` already demonstrates the `source llm-switch.sh` pattern). | One of the two port tables is guaranteed to rot. |
| S3 | `is_llama_cmd` now excludes `.swarmlet` by path **and** `IGNORE_PORTS` names 8100. Delete one (keep the path match — it survives a port move). | Two mechanisms, one boundary; the port list is documented as the one that "rots". |
| S4 | `bin/tune-probes.json` `code-lru-long.prompt` is byte-identical to `bin/bench-model.sh`'s `PROMPT` (174 B, checked equal); `code-diff`'s message embeds `bin/probe-diff.sh`'s `SRC` block (316 B, verified substring). Keep one copy + a pointer. | A fixture edited in one place silently stops gating what it claims to gate. |
| S5 | `cleanup.sh`: either use `$PORTS` (3 lines) or delete the claim in `CLAUDE.md:130-131`. | A documented safety property implemented by a dead variable is worse than no claim. |
| S6 | `bin/llm-switch-reconcile.sh:7` "all ten … plists" → 4, or better, count them. | A comment that is off by 6 is a comment nobody reads. |

**Outside this repo but required to make `bin/llm-tune` correct:**
`~/.claude/skills/ts-ai-auto-improve/scripts/tune-run.sh:517` prints
`cache-ram=${TUNE_CACHE_RAM:-?}GiB` where `TUNE_CACHE_RAM` is parsed from `--cache-ram 4096`,
which is **MiB** — `preflight-fit.sh:164` labels the identical number `MiB`. A 1024× unit error on a
line that ends "(read back from the serve script, not guessed)".

---

## 5. ACCELERATED

| # | win | evidence |
|---|---|---|
| A1 | **Collapse the two log rotators into one 5 MB rotator.** 34 of 46 MB sit in exactly two files, both parked just under the fleet rotator's 20 MB trigger and both caught by cleanup's 5 MB trigger. | `find logs -maxdepth 1 -name '*.log' -size +5M \| wc -l` → `2`; `… -size +20M \| wc -l` → `0`; `ls logs/*.log.gz \| wc -l` → `0` |
| A2 | `bin/llm-switch.sh status` does a full `lsof` sweep + one `ps` per unknown listener (21 lines of output, 18 of them noise). Two of the 18 are the same pid listed twice (51745 on 47800 **and** 47801). Dedupe by pid. | `LLM_SWITCH_VERBOSE=1 bin/llm-switch.sh status` |

Nothing else. The surviving cycle time is dominated by a 4–8 minute model load (`llm-switch.sh:238`
already waits up to 12 min), which is a hardware constant, not a process defect. **Pruning was the
win.**

---

## 6. AUTOMATED

| safe now | why it is safe |
|---|---|
| A read-only `llm-switch list` that asserts (a) every `models/` dir has a lane or a documented reason not to, (b) every `com.lotar.llm-*.plist` maps to a lane, (c) exactly one plist is enabled. | Pure reads; it is the command CLAUDE.md **already promises** (M1) and the only artefact that would have caught M3 (qwen38) and M5. It must be a report, never a fix. |
| A monthly `models/`-vs-lane inventory as a printed diff. | Reading `ls`/`du` only. M4 (38.1 GB of unreferenced dirs) is invisible today. |

| must wait | why |
|---|---|
| Anything that promotes a tune winner automatically (`llm-tune land --apply`). | `logs/tune/ledger.tsv` holds **1** row and it is `VOID-BUDGET`. Automating promotion from a corpus of zero valid measurements cements the process before it has ever worked. |
| Auto-cleanup of `logs/`. | Two rotators disagree on threshold (5 MB vs 20 MB) and the in-repo one skips nothing while claiming to skip the serving model (R8). Fix the rule first. |
| Auto-starting or reaping lanes. | Sole-residency decisions on a 128 GB box shared with a live mesh belong to a human; `d10af40` landing mid-audit is exactly why. |

---

## 7. SCOPE

**In my lane:** delete the two absent lanes' registry/serve/plist surface (R2–R4, D7–D8) and the 6
Class-A dead scripts (D1–D6, 88 KB + 60 KB cache), collapse the two log rotators (R9, A1), and add
the read-only `llm-switch list` the docs already promise (R1, M1) — as a **report**, never a fixer.

**Handoffs (one line each, not my call):**
- `~/.hermes/config.yaml:42,52,62,72,82,92` — 6 local providers on 8088/8091/8093/8094/8097/8099, **all six ports have no listener** → hermes lane.
- `com.fleet.llm-log-rotate.plist` → `ai-fleet/fleet/bin/llm-log-rotate.sh`, threshold 20 MB, 0 archives ever → fleet lane.
- Mesh engine pid moved **80707 → 35099 → 30373** between 10:38 and 11:11 → mesh lane.
- `bin/h3-video.sh` + `bin/music3-*.py` (0 refs, unrelated to serving) → whoever owns `h3.c/`, `music3-venv/`.
- `tune-run.sh:517` `GiB`/`MiB` label bug → ts-ai-auto-improve lane.
- Root `llama.cpp-b10330/`, `llama.cpp-pr27739/`, `llama.cpp-rpc/`, `ds4/`, `ds4-main-tree/`, `h3.c/`, `.hermes/`, `docs/`, `evidence/`, `evals/` — untracked build trees at root, outside this lane's 114 tracked files.

---

## 8. BLOCKERS

**Incident disclosure (asked for by the parent).** My first command block ran
`PRINT=1 bin/serve-gemma4.sh` and `PRINT=1 bin/serve-v4flash.sh`. **Those filenames do not exist** —
the launchers are `serve-gemma.sh` and `serve-v4.sh`. Observed output, verbatim:

```
=== serve-gemma4.sh PRINT=1 ===
/bin/bash: bin/serve-gemma4.sh: No such file or directory
rc=127
=== serve-v4flash.sh PRINT=1 ===
/bin/bash: bin/serve-v4flash.sh: No such file or directory
rc=127
```

No serve script was executed and no engine was started. Verified after the fact:
`ls bin/serve-gemma4.sh bin/serve-v4flash.sh` → `No such file or directory` (rc=1);
`pgrep -fl llama-server` → exactly 3 processes: ollama pid 12551 (:50084), the swarmlet engine
(:8100), and a sibling auditor's watch loop (pid 28006, matched only because its *command text*
contains the string); `for p in 8089 8090 8095 8098 8099; do lsof -nP -iTCP:$p -sTCP:LISTEN -t; done`
→ all empty. **I ran no serve script at any point**; only `serve-flashnext.sh`, which has a
verified print-and-exit guard at line 82 (`if [ "${PRINT:-0}" = 1 ]; then printf …; exit 0; fi`)
before `exec`.

| could not verify | what I would need |
|---|---|
| `bin/llm-switch-test.sh` (38 199 B, the only regression suite for this lane) | The mid-run rule forbids launchctl. Hermeticity **read-verified**: all 4 doubles exist in `bin/llm-switch-test-stubs/`; `run()` prepends `STUBBIN` to `PATH`; the only absolute-path call in `llm-switch.sh` is `HPY=$HOME/.hermes/…/python` (line 279), which the test neutralises by exporting `LLM_SWITCH_NO_HERMES_SYNC=1` at line 24; no test covers the `.swarmlet` class (0 matches for `swarmlet` in the suite). Safe invocation once permitted: `LLM_SWITCH_NO_HERMES_SYNC=1 bin/llm-switch-test.sh` from a shell with `launchctl` proven-shimmed. |
| `bin/llm-switch-reconcile.sh --dry-run` | Invokes `launchctl print-disabled` → blocked by the same rule. `--dry-run` never signals a process (read at lines 36-45); unverified in execution. |
| Static analysis | `shellcheck` is not installed (`command -v shellcheck` → empty). `brew install shellcheck` would give `shellcheck -S warning bin/*.sh`. |
| Whether D8 (`serve-v4.sh`) is safe to remove | An owner decision: the artifact is gone, but 8077 B of SHA-pinned config is the only record of that lane. |
| Whether D10 (`run-dev.sh`, `run-dev-64k.sh`) is safe to remove | Confirm with the SWE-bench/eval owner — CLAUDE.md:34-42 protects that class of file. |
| `logs/*.err.log` truncation is safe while launchd holds the fd | `: > file` keeps the inode and the append fd valid; `llm-log-rotate.sh:91` uses the same idiom and documents the reasoning. Not executed (would need `--apply`). |
