# L6 — swarmlet control plane (`/Users/lotar/projects/ai-mesh/swarmlet/control`)

Read-only audit. Nothing started, stopped, deleted or POSTed. Live plane was GET-only.
Working tree was **mutating underneath me** during the audit — `control/deployments.ts` gained 100 lines at 11:02 (see §1.7).

---

## 1. E2E CHECK

### 1.1 `bun run test:control` — works, 19 failures, 18 of them stale expectations

```
cd /Users/lotar/projects/ai-mesh/swarmlet && bun run test:control 2>&1 | tail -8
```
```
 212 pass
 19 fail
 3319 expect() calls
Ran 231 tests across 25 files. [18.12s]
error: script "test:control" exited with code 1
```
Verdict: **works** (suite executes, 91.8% green). The brief said "~213 pass / 18 fail" — actual **212 / 19**. The 19th failure is not in `planner.test.ts` (see §1.5).

### 1.2 `bunx tsc --noEmit` — **clean**

```
cd /Users/lotar/projects/ai-mesh/swarmlet && bunx tsc --noEmit; echo "TSC_EXIT=$?"
```
```
TSC_EXIT=0
```
No output. Verdict: **works**.

### 1.3 Live inventory + live deployment (GET only) — works

```
TOKEN=$(python3 -c "import json;print(json.load(open(\"/Users/lotar/.swarmlet/control/control.json\"))[\"adminToken\"])")
curl -s -H "Authorization: Bearer $TOKEN" https://app.swarmlet.ai/api/nodes       > nodes.json
curl -s -H "Authorization: Bearer $TOKEN" https://app.swarmlet.ai/api/deployments > deployments.json
curl -s -H "Authorization: Bearer $TOKEN" "https://app.swarmlet.ai/api/assignments?deployment=dep-4bcfade6e039" > assignments-live.json
curl -s -H "Authorization: Bearer $TOKEN" "https://app.swarmlet.ai/api/events?limit=2000" > events.json
```

Node inventory — **7 enrolled, 5 online**:

| id | hostname | os | online | ramMiB | gpus | models | offer roles (w/c/r) |
|---|---|---|---|---|---|---|---|
| `01f78366eb893349` | LAPTOP-PPN32FP0 | win32 | yes | 7857 | 0 | 1 | n/n/**y** |
| `30f05a2670c368d0` | Lotars-MBP.home | darwin | yes | 131072 | 1 (metal:0, 110100 MiB) | 12 | y/y/y |
| `26bc380240373930` | **MBP-od-Veimir** | darwin | **no** | 24576 | 1 | **0** | y/n/n |
| `19d7c8f75e54726a` | lotar-legion | linux | yes | 15894 | 1 (cuda:0, 3604 MiB) | 2 | y/y/y |
| `6474b864aaf6fa5d` | lotar-legion-2 | linux | yes | 15347 | 1 (cuda:0, 3600 MiB) | 2 | y/y/y |
| `6d485f5826267037` | mac | darwin | yes | 16384 | 1 | 1 | y/n/n |
| `e5b13bdc680fcb01` | mac | darwin | **no** | 16384 | 1 | 0 | — |

Live deployment — **exactly one of 88 rows is live**:

| field | value |
|---|---|
| id | `dep-4bcfade6e039` |
| spec | profile `qwen38-27b-q8`, kind `replica`, ctx 131072, parallel 4, chain 0, transport auto, `autoModel: true` |
| state | `ready` |
| coordinator | `30f05a2670c368d0` (Lotars-MBP.home), device `MTL0` |
| endpoint | node `30f05a2670c368d0`, port **8100**, modelName `qwen3.8-27b` |
| plan reasons | "Replica Lotars-MBP.home keeps 64 of 64 layers on MTL0: 64 × 389 + 12288 MiB host = **37184 MiB of 118784 MiB** RAM offered" |
| live assignment | `as-fcdb56dd88df`, state `ready`, detail `soft rss cap 116.0 GiB (watchdog)` |
| createdAt → updatedAt | 2026-09-15T11:05:04Z → 2026-09-16T08:33:30Z |

Other 87 deployments: **all `stopped`** (created 2026-09-04 → 2026-09-15). No pruning exists.

### 1.4 Planner re-run against the LIVE node snapshot — works, and finds a live regression

```
cd /Users/lotar/projects/ai-mesh/swarmlet && bun run /tmp/e2e-audit/L6/plan-live.ts
```
```
profiles: flash-next-ud-q4kxl, qwen35-2b-q8, qwen36-35b-a3b-q4km, qwen38-27b-q8
live nodes: 7 online: 5

[OK] live prod spec as replica: coord=30f05a26 dev=MTL0 rpc=0 split=[] ctx=131072 par=4 chain=0
[OK] live prod spec as split:   coord=30f05a26 dev=MTL0 rpc=2 split=[5,5,54] ctx=131072 par=4 chain=0
[OK] flash-next replica on M5:  coord=30f05a26 dev=MTL0 rpc=0 split=[] ctx=1536 par=1 chain=0

[REFUSED] flash-next split chain 4 (the measured best):
   - No envelope row of profile flash-next-ud-q4kxl fits ctx 1536, parallel 3, chain 4 on 2 worker(s).
   - lotar-legion: 3604 MiB offered on CUDA0 allows at most 1 layer(s) of 1608 MiB after the 1536 MiB margin.
   - lotar-legion-2: 3600 MiB offered on CUDA0 allows at most 1 layer(s) of 1608 MiB after the 1536 MiB margin.
   - Envelope row workerLayers 1 (maxCtx 262144, maxParallel 3, maxChain 0): chain 4 > maxChain 0.
   - Envelope row workerLayers 1 (maxCtx 262144, maxParallel 1, maxChain 0): parallel 3 > maxParallel 1, chain 4 > maxChain 0.

[OK] flash-next split chain 0: coord=30f05a26 dev=MTL0 rpc=2 split=[1,1,46] ctx=1536 par=3 chain=0
```
Verdict: **broken** for one case. The shipped `flash-next-ud-q4kxl` profile refuses the configuration its own README calls "the measured best exact configuration … chain 4 with batched boundary GETs, 12.6 tok/s per stream" (`control/profiles/README.md:27-28`). Every chain > 0 split of the top-ranked model (rank 40) is now unplannable.

### 1.5 The 19 failures, classified

| # | test | file:line | class | cause |
|---|---|---|---|---|
| 1 | `profiles > the shipped profiles load with the measured envelope` | planner.test.ts:64-65 | B | `coordinatorHostMiB` 2048→32768; envelope rows rewritten |
| 2 | `profiles > the 27B profile … names match what the planner matches on` | planner.test.ts:84,86 | B | 27B ladder changed; `maxCtx` 32768→262144 |
| 3-14 (12) | `split placement on the real rig > …` | planner.test.ts:142-267 | B | all blocked by the same envelope row change |
| 15-16 (2) | `windows node placement > Flash-Next …` | planner.test.ts:323,363 | B | envelope change + `coordinatorHostMiB` |
| 17-18 (2) | `replica placement > …` | planner.test.ts:678,688 | B | `coordinatorHostMiB` 2048→32768 in the message text |
| 19 | `500-node pool and 20 concurrent deployments stay bounded …` | fleet.test.ts:90 | **B — different defect** | wall-clock assertion `elapsed < 5000`; observed **5605.98 ms** in the suite and **5568.33 ms** in isolation. Not an expectation drift — a flaky *performance* assertion in a shared, loaded machine. |

`bun test control/test/fleet.test.ts` alone: `14 pass, 1 fail` → reproduces in isolation, so it is not an artifact of parallel test load. Verdict: **flaky** (load-dependent), needs the assertion dropped or the budget raised, not "fixed to 5000".

### 1.6 REQUIRED `planner.test.ts` expectation changes (owner: the session holding the claim — I did not touch the file)

Shipped values are from `control/profiles/*.json` as of 2026-09-16 11:04.

| test literal (current) | shipped value | action |
|---|---|---|
| `planner.test.ts:64` `coordinatorHostMiB: 2048` | `32768` | change literal → `32768` |
| `planner.test.ts:65` `expect(flash.envelope).toEqual([{workerLayers:1,maxCtx:1536,maxParallel:3,maxChain:8},{workerLayers:1,maxCtx:1536,maxParallel:1,maxChain:12}])` | `[{workerLayers:1,maxCtx:262144,maxParallel:3,maxChain:0},{workerLayers:1,maxCtx:262144,maxParallel:1,maxChain:0}]` | change rows → **but this destroys the test's purpose; see note** |
| `planner.test.ts:84` `toEqual([5,10,12,15,20,25,30,35,42])` | `[5,6,8,10,12,15,20,25,30]` | change literal |
| `planner.test.ts:86` `for (const row of q.envelope) expect(row.maxCtx).toBe(32768)` | all rows `262144` | `toBe(262144)` |
| `planner.test.ts:54` `spec()` fixture default `ctx:1536, parallel:3, chain:4` | profile admits only `chain 0` | **fixture must become `chain: 0`** — otherwise 12 tests stay red regardless of literals |
| `planner.test.ts:142-162` expects `p.mtpPath === MTP.path`, `tensorSplit [1,1,46]`, `/row workerLayers 1 \(maxCtx 1536, maxParallel 3, maxChain 8\) fits/`, `/keeps 46 of 48 layers on MTL0: 46 × 1608 \+ 2048 MiB host = 76016 MiB/` | rows are `maxCtx 262144, maxChain 0`; host 32768 → `46 × 1608 + 32768 = 107136 MiB`; `mtpPath` is only set when `chain > 0`, so with `chain 0` it is `undefined` | row label → `(maxCtx 262144, maxParallel 3, maxChain 0)`; 76016 → **107136**; drop `mtpPath` assertion or set `chain ≥ 1` **which the profile now refuses** |
| `planner.test.ts:168` `/\(maxCtx 1536, maxParallel 3, maxChain 8\): chain 10 > maxChain 8/` | `/\(maxCtx 262144, maxParallel 3, maxChain 0\): chain 10 > maxChain 0/` | change literal |
| `planner.test.ts:171` `/(maxCtx 1536, maxParallel 1, maxChain 12): parallel 3 > maxParallel 1/` | `(maxCtx 262144, maxParallel 1, maxChain 0)` | change literal |
| `planner.test.ts:176-180` `parallel 1 chain 12 fits the second row` | no row admits any chain > 0 | test is **unreachable by design now** — delete or invert to a refusal |
| `planner.test.ts:184` `/ctx 2048 > maxCtx 1536/` | `maxCtx 262144` → ctx 2048 is now *admitted* | invert: `expect(plan({spec: spec({ctx: 2048, chain: 0})})).toMatchObject({ctx: 2048})` |
| `planner.test.ts:259` `46 × 1608 \+ 2048 MiB host = 76016 MiB` | `46 × 1608 + 32768 = 107136 MiB` | change literal |
| `planner.test.ts:684` `48 × 1608 \+ 2048 MiB host = 79232 MiB of 110000 MiB` | `48 × 1608 + 32768 = 109952 MiB of 110000 MiB` | change literal (and note: 109952 vs 110100 real offer — a 148 MiB margin) |
| `planner.test.ts:692` `48 × 1608 \+ 2048 MiB host = 79232 MiB exceeds the 70000` | `109952 MiB exceeds the 70000` | change literal |
| `planner.test.ts:363` `/48 × 1608 \+ 2048 MiB host = 79232 MiB exceeds the 12288 MiB RAM offered/` | `109952 MiB exceeds the 12288` | change literal |

Note on `planner.test.ts:65`: mechanically editing it makes the file green while disabling the only test that pins the measured flash-next envelope (`chain ≤ 8 @ parallel 3`, `chain ≤ 12 @ parallel 1`). The row values should be decided before the literals are edited — see §3.1.

### 1.7 The churn: a ready deployment re-placed 85× in 21.8 h (the parent's question)

**Counts (live `events.json`, `dep-4bcfade6e039`, 2026-09-15T11:05:04Z → 2026-09-16T08:53:30Z = 21.81 h):**

| metric | count | command |
|---|---|---|
| events for this deployment | **670** | `python3 -c "… len([x for x in e if x['deploymentId']=='dep-4bcfade6e039'])"` |
| `re-placing after` events | **85** | grep `'re-placing after'` |
| of those, `before == after` (**literal no-op**) | **65** | regex `\((.*) -> (.*)\)`, compare |
| of those, layout actually changed | **20** (10 → split, 10 → replica) | same |
| `ready (replica on …)` | **79** | engine reloads |
| `… starting` | **120** | distinct `as-*` ids = **120 assignments** |
| `automatic model choice is now …` | **22** = 11 `split` + 11 `replica` | oscillating replica↔split |
| `failed:` incl. SIGABRT | **18** | of which **8** are `code 134, SIGABRT` in `ggml_backend_rpc_add_server` |
| assignment rows now | **120** (119 retired, 1 live) | `/api/assignments?deployment=…` |
| interval between re-placements | min 2.6 / median **15.8** / max 42.5 min; <5 min: 10, 5-15: 20, ≥15: 54 | |

**Trigger breakdown (85 events):** `69 × "MBP-od-Veimir joined"`, `10 × "node 26bc380240373930 went offline"`, `3 × "mac joined"`, `1 × LAPTOP-PPN32FP0`, `1 × lotar-legion-2`, `1 × Lotars-MBP.home`.
→ **79 of 85 (93 %) of all re-placements are caused by ONE node — `26bc380240373930` / MBP-od-Veimir — flapping online/offline.** That node is offline right now, holds **0 models**, and offers only the `worker` role.

**The exact code path** (`control/deployments.ts`, line numbers as read at 10:5x, before the 11:02 edit):

1. `server.ts:308` — `hooks.onHello = (nodeId, hello) => { … deployments.onNodeOnline(nodeId) }`. Fires on **every** agent hello (`channel.ts:138`), i.e. every WS (re)connect. Not a timer.
2. `deployments.ts:356 onNodeOnline()` — moves only if `dep.state === "ready"` **and** pacing passed:
   - `deployments.ts:361` `Date.now() < (this.lastMove.get(dep.id) ?? 0) + this.moveIntervalMs` → default **10 min** (`deployments.ts:104`)
   - `deployments.ts:362` `Date.now() - Date.parse(dep.updatedAt) < (this.deps.moveSettleMs ?? 60_000)` → **60 s settle**
   - `deployments.ts:363` `if (dep.spec.autoModel) { … void this.redistribute(dep.id, \`${joined} joined\`) }` — **unconditional, no gain check**
   - `deployments.ts:372-374` (the non-auto path) **does** gate: `const gain = this.placementGain(…)` (`deployments.ts:467`, `moveMarginLayers = 2` at `:108`)
3. `deployments.ts:549 redistribute()` — re-plans and then unconditionally `await this.start(id, true, undefined, { abandonOfflineCleanup: true, fromReady: true })`. `start(…, fromReady: true)` explicitly admits `["ready","placing","loading"]` (`:729`). **There is no comparison of candidate vs current plan.**
4. `main.ts:13-19` — `reconcile()` runs on a **1000 ms** sweeper. `reconcile()` alone cannot move a ready deployment: the wedged block requires `!reconnect && intent.running` and `wedgedLoadReason()` returns null unless `dep.state ∈ {placing, loading}` (`:492-494`). The **only** mover of a ready deployment is `onNodeOnline`.

So: **the comment at `deployments.ts:321` — "redistribute() re-decides and skips the restart when nothing would actually change" — was false.** The evidence is 65 events whose own text is `(Lotars-MBP.home -> Lotars-MBP.home)`.

**(1) Is a ready deployment supposed to be movable on inventory churn?**
Per the documented policy — **no**. `README.md:194-197`: *"A node arrives. onNodeOnline asks whether the nodes here now would place the deployment better. It moves only for a material gain: at least `moveMarginLayers` (2) fewer layers on the serving node, or a coordinator with >=25% more offered RAM."* The code applies that rule only to the non-`autoModel` branch (`:372`). For `autoModel` it is absent. The live deployment is `autoModel: true`. **Code contradicts its own README for exactly the one deployment that exists in production.**

**(2) Why does the 4-way RPC split abort?**
8/10 split attempts died with `code 134, SIGABRT` 38-47 s after spawn, all in `ggml_backend_rpc_add_server`. Two distinct traces:
```
common_params_parser_init → …$_1438__invoke → ggml_backend_rpc_add_server + 552 → common_params_parse
common_init_result ← common_init_from_params ← server_context_impl::load_model ← llama_server
```
The spawned argv (agent.err.log:42406, 42451):
```
llama-server -m …/Qwen3.8-27B-Q8_0.gguf --host 127.0.0.1 --port 8100
  --rpc 127.0.0.1:59293,127.0.0.1:59294,127.0.0.1:59295 --device RPC0,RPC1,RPC2,MTL0
  --tensor-split 5,5,5,49 -ngl 999 -c 131072 --parallel 4 …
```
The three `127.0.0.1` ports are **not** local workers — `TunnelPool.localPort()` (`control/tunnel.ts:15-40`) binds `server.listen(0, "127.0.0.1")` on the **control host** and pipes each accepted socket into the remote node's agent channel. So the coordinator dials its remote workers through localhost relays. `deployments.ts:918-931 endpointFor()` still advertises `privateIps`/`publicIp`/`publicEndpoints` as `direct` with `relay: true`, and `SWARMLET_REVERSE_DIAL` is **off by default** (`:917`), so every split today is relay-only.
Root cause of the abort itself: **UNVERIFIED** — would need one controlled repro of `llama-server --rpc 127.0.0.1:<tunnel>` against a live tunnel to capture the ggml assertion. What *is* established: it is deterministically the RPC-server-add step, it never happens for a replica, and the tunnel is the only difference from a same-machine split.

**(3) Smallest change that stops the churn.** Ranked; #1 and #3 are **already being written by another session** (§ below), #2 and #4 are not:

| # | change | kills | where |
|---|---|---|---|
| 1 | **Bail out of `redistribute()` when the candidate is the placement already running.** Compare `planNodes()` + coordinator/device + the worker list + ctx/parallel/chain/tensorSplit. | the 65 no-ops (**76 %** of all moves) | `deployments.ts` `redistribute()`, 1 guard |
| 2 | **Apply the documented `placementGain` gate to the `autoModel` branch of `onNodeOnline()`** — i.e. make the code match `README.md:194-197`. | the remaining replica↔replica and the "better" moves that change nothing material | `deployments.ts:363-365` |
| 3 | **Do not run `fitsNow()` against a plan the deployment is already serving.** `fitsNow()` (`:393-403`) compares modelled need to the node's *live* `metrics.freeRamMiB` — which includes **this deployment's own resident engine**. A 37184 MiB replica needs 37184 MiB free *while it is holding 37184 MiB*; any unrelated RAM dip flips the auto choice to a split. That is the direct cause of the 11 replica→split flips. | the replica↔split oscillation (22 `automatic model choice is now` events) | `deployments.ts chooseAuto()` `:359-362` / `fitsNow()` |
| 4 | Treat a flapping node as one node: `moveExcluded` is keyed per-node and is cleared after 60 s of `ready` (`:189`). A node that has never held the model and flaps should not be a `joined` trigger at all. | the 69 triggers from one broken peer | `onNodeOnline()` / `moveExcluded` |

Do **not** lower `moveIntervalMs` (10 min) — the pacing is not the defect. The smallest *safe* interval is the current one; the sub-10-min gaps in the log (10 of them, 2.6-3.7 min) come from the **unpaced** reconnect-expiry path in `reconcile()` (`:155-163`), which is intentional and correct (grace expired → move).

**In-flight fix observed at 11:02 (not mine, another session):** `git diff -- control/deployments.ts` adds `samePlacement()`, `planFingerprint()`, `blockPlacement()` / `blockedUntil()` / `planFor()` and a 1 h→24 h escalating `crashCooldownMs`. That covers #1 (partial: guarded by `dep.state === "ready"`) and blocks the crashed split fingerprint. **It does not touch `onNodeOnline`, `chooseAuto` or `fitsNow`** — #2 and #4 remain open. Residual gap in the new code: `planFingerprint` is exact-shape, so a *different* split (2 workers instead of 3, different layer counts) has a different fingerprint and is free to crash once more.

**Count: how many assignments would a correct implementation have created in 21.8 h?**

| scenario | assignment rows | basis |
|---|---|---|
| observed | **120** | 120 distinct `as-*` ids in the event log |
| with fix #1 only (skip identical placement) | ~**55** | 20 real moves: 10 splits (4 rows) + 10 replicas (1 row) = 50, +1 initial |
| with #1 + #2 + #3 | ~**20** | the 10 split flips vanish; 10 legitimate node-loss moves remain |
| with #1-#4 and the flapping peer excluded | **1-2** | 26bc380240373930 holds no model and only the `worker` role; with it out of the trigger set nothing else changes in 21.8 h |

### 1.8 `grep` of the recently added paths — as specified

```
grep -rn "WEDGED_LOAD_MS\|wedgedLoadReason\|redistribute\|lastMove" control/*.ts
```
→ 12 hits, all in `deployments.ts` (`:33 WEDGED_LOAD_MS`, `:61 lastMove`, `:65/:106 wedgedLoadMs`, `:156,166,168,169` = the rescue in `reconcile()`, `:317,321,323,332` = `onNodeOnline`, `:448→492 wedgedLoadReason`, `:470→549 redistribute`).

```
grep -rn "upstreamTask\|reverseDial" control/*.ts
```
→ **2 hits, both `reverseDial`** in `deployments.ts:917,943,957`. **`upstreamTask` does not exist anywhere in `control/`.** Verdict: the reverse-dial wiring is a single env-gated `if` (`SWARMLET_REVERSE_DIAL === "1"`), default off, 3 lines; the brief's `upstreamTask` symbol is not in this lane (would be in `node-agent/`).

---

## 2. REQUIREMENTS

Owners: **git author is uniformly `Claude Code <mladen@the-shop.io>`** across all 143 commits — no placement rule in the repo names a human. `planner.test.ts:369` says `// Owner rule: …` but names nobody. So almost everything below is UNOWNED by the rubric's standard.

### 2.1 Placement policy

| # | requirement | owner | keep/drop + what breaks |
|---|---|---|---|
| R1 | Role+model affinity: a node must offer `replica`/`coordinator`/`worker` **and** hold a `ggufPattern` match | UNOWNED | **KEEP.** Without it a placement lands on a node with no weights. `planner.ts:232 requireNode`, `planReplica` |
| R2 | Deterministic node order: replica = first by hostname; workers = RTT then hostname; coordinator = largest RAM offer | UNOWNED | **KEEP.** `planner.ts:193-199, 354-358`. Load-bearing: the test `same input, same plan` depends on it |
| R3 | Ready-gating: only a `ready` deployment is movable; ≥60 s settle | UNOWNED | **KEEP** the gate, **DROP** the value as a hardcoded default — `moveSettleMs ?? 60_000` is a magic number with no measurement behind it |
| R4 | Wedged rescue: a `placing`/`loading` deployment is re-planned after 5 min | UNOWNED (added 2026-09-15, commit `4587df2`, no incident cited) | **KEEP**, but see §3.2 — the "plan names an offline node" half is reachable only after a control restart |
| R5 | RAM shortfall: a **darwin** worker must fit the agent's RAM offer, not only the GPU offer | named in commit `55e0584` ("a darwin worker must fit the RAM offer its own agent enforces") but not by a human | **KEEP.** One incident (30 layers, 18186 MiB GPU / 12288 MiB RAM) but the rule is enforced by real code (`node-agent/enforce/index.ts` RSS watchdog). Class C |
| R6 | Envelope rows: ctx/parallel/chain per worker-layers row | UNOWNED | **KEEP the mechanism, DROP the current values** — see §3.1 |
| R7 | `fitsNow`: darwin replica must fit **live free RAM** before the auto choice commits | UNOWNED; doc cites one incident (`README.md:239-244` "The first time it ran without that check it picked a 110 GiB model on a machine with 65 GiB free and the fit gate killed it seconds later") | **DROP IN ITS CURRENT FORM.** This single incident rule is now the top cause of production churn. Replace with offer-based fit minus the deployment's own resident need (§1.7 #3) |
| R8 | `placementGain` = ≥2 fewer serving layers or ≥25 % more coordinator RAM | UNOWNED | **KEEP** (it is the right rule) — **apply it to the `autoModel` branch where it is currently missing** |
| R9 | `moveIntervalMs` = 10 min floor between moves | UNOWNED | **KEEP.** 10 min is defensible; the median observed interval (15.8 min) shows pacing was never the binding constraint |
| R10 | Pinned specs never move (`pinsNodes`) | UNOWNED | **KEEP.** 4 passing tests depend on it; it is the owner-consent boundary |
| R11 | Recovery budget: `MAX_RECOVERY_ATTEMPTS = 5`, `reconnectGraceMs = 30 s` | UNOWNED | **KEEP.** The 30 s grace is what saves an engine reload on a blip — the log shows the opposite happening only via R3/R7 |
| R12 | `moveExcluded`: a node a move failed on is dropped for 10 min, per deployment | UNOWNED | **CHALLENGE.** Cleared after 60 s of `ready` (`:189`); per-deployment, not per-node. With one flapping peer this is the wrong shape (R4/#4 in §1.7) |
| R13 | `crashCooldownMs` 1 h → ×4 → 24 h cap | in flight at 11:02, **no owner, no test yet** | **UNOWNED — challenge hardest.** A 24 h memory of "this shape crashed" is a durable behaviour lock in memory only (lost on restart, never in the DB). If a crash was a transient tunnel failure the deployment is wedged for a day with a message telling the owner to stop/start |

### 2.2 Rules traceable to one incident that may no longer exist

| rule | incident | still real? |
|---|---|---|
| R5 darwin RAM shortfall | 30-layer worker stopped by the watchdog | **yes** — the watchdog is in shipped code |
| R7 `fitsNow` free-RAM gate | "picked a 110 GiB model on a machine with 65 GiB free" | **the incident is gone** (flash-next is no longer auto-eligible at all) but the rule fires on every hello and now evicts a working deployment |
| R12 `moveExcluded` | "a failed move must not walk back into the node it bounced off" | **yes** for a one-off failure, **wrong shape** for a flapping peer |
| R13 crash cooldown (in flight) | today's SIGABRT | yes for the exact fingerprint; too coarse as a mechanism |

### 2.3 Control HTTP surface — every route and its callers

Route table from `control/server.ts` (`api()` at `:94-195`, perimeter at `:203-283`). Caller greps: `control/ui/*.js|*.html`, `node-agent/**/*.ts` (control-plane calls only — the agent's own `/api/*` in `node-agent/localapi.ts` is a **different server**), `e2e/**`.

| method + path | server.ts | UI caller | agent caller | e2e caller | verdict |
|---|---|---|---|---|---|
| `GET /health` | :207 | — | — | — | used by `control/install-launchd.sh:46`, `cloudflare-tunnel.sh` |
| `GET /releases/*` | :209 | — | ✔ `node-agent/update` | — | live |
| `GET /node-update-lease` | :211 | — | ✔ | — | live |
| `POST /enroll` | :212 | — | ✔ `node-agent/agent.ts:41` | — | live |
| `GET /agent` (ws) | :227 | — | ✔ `agent.ts:49` | — | live |
| `GET /probe/down`, `POST /probe/up`, `GET /probe/ip` | :233-237 | — | ✔ `node-agent/probe/net.ts:60,76,96` | — | live |
| `GET /v1/models` | :243 | — | ✔ `node-agent/ui/chat.js:64` | — | live |
| `GET /v1/mesh` | :248 | — | — | tests only (`processing.test.ts`) | **no runtime caller** |
| `* /v1/*` | :256 | — | — (client-side) | — | live (inference) |
| `POST /login`, `GET /logout` | :259,270 | ✔ `index.html` form | — | — | live |
| `GET /api/telemetry` | :96 | ✔ `telemetry.js:70` | — | — | live |
| `GET /api/fleet` | :107 | ✔ `fleet.js:167` | — | ✔ | live |
| `POST /api/fleet/preview` | :108 | ✔ `fleet.js:245` | — | ✔ | live |
| `GET /api/fleet/:id` | :111 | ✔ `fleet.js:175` | — | — | live |
| `POST /api/fleet/:id/apply` | :112 | ✔ `fleet.js:252` | — | — | live |
| `GET /api/nodes` | :114 | ✔ `app.js:250` | — | ✔ ×12 | live |
| `GET /api/stream` (SSE) | :115 | ✔ `app.js` | — | ✔ | live |
| **`GET /api/nodes/:id`** | **:150** | **none** | **none** | **none** | **DEAD** |
| **`PUT /api/nodes/:id/offer`** | **:151-155** | **none** | **none** | **none** | **DEAD** (and a no-op: it validates then replies `"offers are changed on the node; this only validates"`) |
| `POST /api/join-codes` | :156 | ✔ `app.js:380` | — | ✔ | live |
| `GET /api/events` | :157 | ✔ `app.js:1018` | — | ✔ | live |
| `GET /api/api-keys` | :158 | ✔ `app.js:1042` | — | ✔ | live |
| `POST /api/api-keys` | :159 | ✔ `app.js:1055` | — | ✔ | live |
| `GET /api/assignments/:id/logs` | :160 | ✔ `app.js:626` | — | — | live |
| `GET /api/assignments` | :161 | ✔ `app.js` | — | ✔ | live |
| `GET /api/profiles` | :162 | ✔ `app.js:432,694,1317`; `fleet.js:195` | — | — | live |
| `GET /api/routing` | :163 | ✔ `app.js:863,1075` | — | ✔ ×9 | live |
| `GET /api/deployments` | :166 | ✔ `app.js:390` | — | ✔ | live |
| `POST /api/deployments` | :167 | ✔ `app.js:843` | — | ✔ | live |
| `POST /api/deployments/plan-preview` | :168 | ✔ `app.js:502,823` | — | — | live |
| `GET /api/deployments/:id` | :169 | ✔ `app.js:567,1480` | — | ✔ | live |
| `PUT /api/deployments/:id/distribution` | :170 | ✔ `app.js:511` | — | ✔ | live |
| `POST /api/deployments/:id/distribution/apply` | :173 | ✔ `app.js:526` | — | ✔ | live |
| `POST /api/deployments/:id/start` | :177 | ✔ `app.js:846` | — | ✔ | live |
| `POST /api/deployments/:id/stop` | :181 | ✔ `app.js:535` (`act(d,'stop')`) | — | ✔ | live |
| `DELETE /api/deployments/:id` | :182 | ✔ `app.js:535` (`act(d,'delete')`) | — | ✔ | live |
| `GET /api/whoami` | :192 | ✔ `app.js:1559` | — | — | live |

```
grep -rn "api/nodes/" . --include=* | grep -v node_modules | grep -v Binary     → (no output)
grep -rn "nodes/.*offer\|/offer'" --include=*.ts --include=*.js --include=*.py --include=*.sh . → (only node-agent's own PUT /api/offer)
```
**2 of 30 routes have zero callers anywhere in the repo.**

---

## 3. DELETED

### 3.1 `control/profiles/flash-next-ud-q4kxl.json` — restore the measured envelope. Class **B**

| field | before `d0fc32a` (2026-09-15) | shipped now | documented in `profiles/README.md:20,23,24` | verdict |
|---|---|---|---|---|
| `coordinatorHostMiB` | 2048 | **32768** | **2048** ("the 28.8 GiB PLE n-gram table stays mmap-backed … deliberately **NOT** part of the fit gate") | shipped value contradicts its own doc; revert |
| `envelope[0].maxCtx` | 1536 | **262144** | 1536 | 170× inflation |
| `envelope[0].maxChain` | 8 | **0** | 8 | the measured-best `chain 4` is now refused |
| `envelope[1].maxChain` | 12 | **0** | 12 | same |

Consequences proven live (§1.4): the top-ranked profile (rank 40) is refused by the auto chooser with `flash-next-ud-q4kxl needs 109952 MiB free on Lotars-MBP.home, which has 64876` — **22 occurrences** in the last 21.8 h — and `flash-next split chain 4` is unplannable.
Also inconsistent: `profiles/README.md:85` says the 27B `coordinatorHostMiB` is **6144**; `qwen38-27b-q8.json` ships **12288**, and 12288 is what the live plan uses (37184 MiB).
Evidence: `git show d0fc32a -- control/profiles/flash-next-ud-q4kxl.json`; `grep -n coordinatorHostMiB control/profiles/README.md`.
Risk if wrong: if 32768 was a deliberate new measurement, reverting re-admits a config that OOMs. **Restoration:** `git checkout d0fc32a^ -- control/profiles/flash-next-ud-q4kxl.json` (not run — no git mutations).
Size: 755 B. The 18 stale `planner.test.ts` expectations are the same defect viewed from the test side: **fix the profile (or decide it is right) before editing the literals.**

### 3.2 `control/server.ts:150` `GET /api/nodes/:id` and `:151-155` `PUT /api/nodes/:id/offer` — Class **A**

| item | size | evidence | risk | restore |
|---|---|---|---|---|
| `GET /api/nodes/:id` | 203 B (1 line) | `grep -rn "api/nodes/" . --include=* \| grep -v node_modules` → empty; no `nodesSnapshot`-by-id user in `ui/` | low — the UI reads the whole list and filters client-side | re-add 1 line |
| `PUT /api/nodes/:id/offer` | 419 B (5 lines) | same grep; it only *validates* and the real mutation lives on the node (`node-agent/localapi.ts:61`) | low — deleting removes a validation echo nobody consumes | re-add 5 lines |

### 3.3 `protocol/sign.ts:38-40` `fingerprintDer` — Class **A**

```
grep -rhoE "\bfingerprintDer\b" --include=*.ts --include=*.js control protocol node-agent shared e2e | wc -l  → 1
```
1 occurrence = the declaration. 164 B. Risk: none found. Restore: re-add 3 lines.
(Cross-lane: `protocol/` is shared with the node-agent lane — hand off, do not delete unilaterally.)

### 3.4 Delivery bookkeeping — propose, do not delete

| item | size | evidence | class |
|---|---|---|---|
| 87 of 88 deployments retained forever, all `stopped` | 218 KB JSON | `/api/deployments` → `Counter({'stopped': 87, 'ready': 1})` | **B** — no retention rule exists. Not deletable by me (it is another owner's history) |
| `/Users/lotar/.swarmlet/control/control.sqlite` (4.2 MB) + `control.err.log` (4.7 MB) | 8.9 MB | `sqlite3 -readonly … "SELECT count(*) FROM deployments"` → **3**, newest `2026-09-10`; live plane has 88 and is *not* this file | **B** — stale local control-plane state that will mislead any local `SELECT`-only investigation (it misled this one for two commands) |
| `dist/` 1.3 GB | gitignored, regenerable | `du -sh dist` | **A** for storage, but **not my lane** — the agent build owns it |

### 3.5 The ~10 % I expect to come back

1. `PUT /api/nodes/:id/offer` — an operator debugging an offer will want a validation endpoint again. Add it back in the form that matters: one that actually forwards the change, or none.
2. `chain > 0` flash-next splits — deleting `maxChain 0` restores a capability that crashed the ring before (`README.md:23`: "chain 10 at `--parallel 3` failed to allocate the RPC0 compute buffer"). Expect `maxChain: 8` to be re-tightened, not deleted.
3. `GET /api/nodes/:id` — cheap to re-add for a per-node drawer.

---

## 4. SIMPLIFIED

Survivors only.

| # | change | file | why |
|---|---|---|---|
| S1 | Delete the `export` keyword on `DEFAULT_PARALLEL`, `DEFAULT_CHAIN`, `WORKER_PORT_BASE`, `WORKER_PORT_STEP` — each is used only inside `planner.ts` (lines 225, 591, 592) and by no test | `planner.ts:37-42` | 4 exported constants that advertise an API nobody imports |
| S2 | Fold the duplicated `if (this.operations.has(id)) return false;` — written twice, now the 2nd at `redistribute()` after the `pinsNodes` early return | `deployments.ts redistribute()` | one branch, not two |
| S3 | Move `profiles/README.md`'s flash-next table (lines 20-24) and 27B table (lines 85-88) to be **generated from or asserted against** the JSON | `planner.ts loadProfiles` | three-way drift (JSON / README / test) is the root defect of §3.1. A `loadProfiles` assertion that the README's quoted `coordinatorHostMiB` equals the JSON's would have failed loudly on `d0fc32a` |
| S4 | Replace the wall-clock assertion in `fleet.test.ts:90` (`elapsed < 5000`) with a **work-bound** assertion (`evaluatedCandidates < 12000`, already there) | `control/test/fleet.test.ts:90` | a wall-clock bound on a shared, inference-serving box is not a test, it is a coin flip. Observed 5568-5606 ms both in-suite and isolated |
| S5 | Give `onNodeOnline()`'s `autoModel` branch the same 5-line shape as the branch below it (`plan` → `placementGain` → `redistribute`) | `deployments.ts:363-365` vs `:366-375` | one policy, two code paths, one of which ignores the policy |

---

## 5. ACCELERATED

**Pruning was the win.** Concretely, in 21.8 h of live history: 65 of 85 re-placements had a literally identical before/after and cost an engine reload; the fix is a guard, not a scheduler change. `bun run test:control` runs in 18.1 s across 231 tests — no cycle-time problem there.

Two real ones:

| # | change | win |
|---|---|---|
| A1 | `reconcile()` runs on a **1000 ms** sweeper (`main.ts:13`) and iterates **all 88 deployments** every tick, calling `reg.listDeployments()` + `deploymentIntent()` + `listAssignments()` per deployment. At 88 rows it is fine; the storage has no retention (§3.4) so it grows monotonically | ~88 full row-loads/s. Bound the sweep to deployments with `intent.running` or not `stopped`, or move the interval to 5 s |
| A2 | The stale `planner.test.ts` blocks `test:control` from ever being green, so **nobody runs it as a gate**. 18 stale literals is 18.1 s of signal nobody can use | make `test:control` a gate again by applying §1.6, then wire it |

---

## 6. AUTOMATED

| safe now | why | must wait |
|---|---|---|
| A dashboard/diagnostic that counts `before == after` re-placements per deployment per day | purely read-only over `reg.listEvents`; would have surfaced the 65 no-op moves on day 1 | — |
| An assertion in `loadProfiles()` that `profiles/README.md`'s quoted numbers match the JSON | the drift in §3.1 is mechanical and detectable | — |
| A units test asserting `test:control` is green | needs §1.6 first | ✗ |
| Auto-retention of `stopped` deployments older than N days | **must wait** — it deletes another owner's history and the fleet UI shows placement history from those rows | ✗ |
| Auto-apply the `crashCooldownMs` shape-block into a persistent store | **must wait** — the in-flight fix keeps `blocked` in memory only; persisting an automatic "never place X again" before the mechanism has an owner and a test cements an unowned rule (§2.1 R13) | ✗ |
| Auto-tuning `moveIntervalMs`/`moveSettleMs`/`wedgedLoadMs` | **must wait** — none of the three has a measurement behind it; automating them now would cement three magic numbers | ✗ |

---

## 7. SCOPE

**Do in this lane:** (a) decide and restore the `flash-next-ud-q4kxl` envelope + `coordinatorHostMiB` (§3.1) and then apply the §1.6 test literals; (b) add the plan-equality guard *and* the `placementGain` gate to `onNodeOnline`'s `autoModel` branch, and stop `fitsNow` from counting the running deployment's own footprint (§1.7 #2-#3); (c) delete the two un-called routes (§3.2); (d) drop `elapsed < 5000` from `fleet.test.ts:90`.

**Handoff, not mine:**
- `protocol/sign.ts:38 fingerprintDer` is dead — the protocol/node-agent lane owns that file.
- `dist/` is 1.3 GB (agent build lane).
- `node-agent/nat.ts` + the `download`/offer fetch paths (`node-agent/localapi.ts:75-83`, `node-agent/probe/models.ts`) are the node-agent lane's archaeology; the only control-side coupling I found is `endpointFor()`'s `publicEndpoints` (`deployments.ts:928-931`), which is consumed and live.
- The 4-way RPC SIGABRT is a llama.cpp/relay question; the control-side half (relay-only, `reverseDial` default off) is documented in §1.7(2).

---

## 8. BLOCKERS

| # | unverified claim | what I would need |
|---|---|---|
| B1 | the exact ggml assertion behind `ggml_backend_rpc_add_server + 552` | one controlled repro of `llama-server --rpc 127.0.0.1:<tunnel-port>` against a live tunnel. **Blocked by the no-start rule** — and by the live deployment |
| B2 | whether `coordinatorHostMiB 32768` was a new measurement or a mistake | the author of `d0fc32a` (git says `mladen@the-shop.io`, uniformly across all commits). No doc, note or test cites 32768 |
| B3 | true per-assignment creation times | `assignments` has **no `created_at`** (`registry.ts` schema: `id, deployment_id, node_id, body, state, detail, updated_at, retired`), and `updated_at` is bulk-touched (87 rows share `2026-09-16T08:42`). I counted from the **event log** (120 distinct `as-*` ids) instead |
| B4 | the live plane's row counts from the DB | `app.swarmlet.ai` is **not** served by `/Users/lotar/.swarmlet/control/control.sqlite` (3 deployments, newest 2026-09-10). No local DB read gives live truth; everything here is from `/api/*` |
| B5 | the in-flight `deployments.ts` edit's final shape | the working tree changed at 11:02 mid-audit (`git status` → `M control/deployments.ts`, +100 lines). All `deployments.ts` line numbers cited above are from the tree as read at ~10:50; after the edit `onNodeOnline` moved from `:312` to `:356`. Re-verify before applying §1.7's ranking |
| B6 | whether `wedgedLoadReason()`'s "plan names an offline node" branch can still fire | I traced it to one reachable path — **after a control restart**, `restore()` sets `state: "loading"` with **no** `reconnecting` record, so a plan naming a then-offline node reaches the branch. It is reachable; I could not construct a second path without restarting control |
