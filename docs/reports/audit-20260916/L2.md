# L2 — documentation vs reality (local-llm: CLAUDE.md, README.md, plans/, docs/, cross-repo pointers)

Docs in scope: `CLAUDE.md` 146 L / 1292 w · `README.md` 531 L / 5058 w · `plans/eval100-plan-*.md` 205 L / 5101 w ·
`plans/HUD_TASK_CONTRACT_v2_*.md` 80 L / 1754 w · `docs/v4-triage-benchmark.md` 58 L / 826 w. Executable-claim ratio: **20 executed / 6 blocked** (blocked = would need a loaded lane; starting one is forbidden).

---

## 1. E2E CHECK

Run from `/Users/lotar/projects/local-llm`. Each row = command → observed.

| # | Claim / command under test | Observed | Verdict |
|---|---|---|---|
| 1 | `bin/llm-switch.sh list` (CLAUDE.md:111 "table: name/port/on-disk size/serving?") | `usage: llm-switch gemma4\|v4flash\|flashnext\|off\|status` exit 2 | **BROKEN** — verb does not exist |
| 2 | `bin/llm-switch.sh status` | 3 lanes all `not loaded … port down` + `ORPHAN pid=80707 port=8100 cmd=…/.swarmlet/releases/…/engine/llama-server -m …/.swarmlet/models/Qwen3.8-27B-Q8_0.gguf` + `18 UNKNOWN-LISTENER` | works |
| 3 | verbs `restart`, `stop`, `logs <name>` | absent from `case` block (`bin/llm-switch.sh:367-388` = `status\|off\|gemma4\|v4flash\|flashnext`) | **BROKEN (3 of 6 documented verbs)** |
| 4 | `bin/llm-lanes` | prints 4 lanes `8099/8095/8098/8089` all `DOWN`; ENGINE column blank when down | works |
| 5 | `curl -m2 -o/dev/null -w %{http_code} 127.0.0.1:<p>/health` for 8099/8095/8098/8089 | all `000` (dead) | doc "flashnext :8099 (PROD)" is **not currently true** |
| 6 | same probe for cross-repo ports: `47800` (swarmlet mesh), `8112`,`8116` (mlx), `11434` (ollama), `8100` | `200 200 200 404(health)/200(models) 200` | all referenced co-tenants exist & are up |
| 7 | `ls models/; du -sh models/*` vs CLAUDE.md:127 "only flashnext … has its GGUF on disk" | 6 dirs / 267 G: flash-next 118G, deepseek-v4-flash-dwarfstar 81G, qwen3.8-27b 30G, qwen3.6-35b-a3b 29G, qwen3.5-draft 9.1G, flash-next-hf 22M | **FALSE** (claim already contradicted in-repo: `plans/eval100-plan-20260902.md:24` "CLAUDE.md:76 + README say deleted — FALSE, weights on disk") |
| 8 | `test -e` on 26 paths the docs name | MISSING: `bin/fetch-glm.sh`, `bin/serve.sh`, `bin/bench.sh`, `bin/bench_refine.sh`, `bin/head2head.sh`, `bin/lossless_check.sh`, `bin/serve-v4-eval.sh`, `/opt/homebrew/bin/llm-switch`, `~/.claude/jobs/d02c4b1e/tmp/evals`, `models/qwen3.6-40b-deckard`; EXISTS: `bin/fetch-model.sh bench-model.sh verify-model.sh set-mem.sh cleanup.sh llm-tune llm-lanes tune-rig.env tune-matrix.env tune-probes.json gguf-meta.py preflight-fit.sh bench-longctx.sh`, both `plans/*.md`, `logs/tune/ledger.tsv`, `evals/` (78 entries), `docs/evidence/*`, `logs/flashnext-mtp-ab-20260909T102100Z/REPORT.md`, `~/.claude/skills/ts-ai-auto-improve/scripts/` (6 files, 2112 L), `~/.hermes/config.yaml` | 10 dead pointers in the docs |
| 9 | `PRINT=1 bin/serve-flashnext.sh` (CLAUDE.md:141 "print the exact command line without loading 111 GB") | prints 24-flag argv, `--ctx-size 262144 --parallel 4 --cache-ram 4096 --predict 52428`, no `--draft-mtp`; exit 0; no load | works; README:370 "predict 32768" is **stale** (actual 52428) |
| 10 | `PRINT=1 bin/serve-flashnext-test.sh` | `--port 8095 --ctx-size 32768 --parallel 1 --alias qwen3.8-flash-next-test` | matches README:372 "wrapper overrides ctx to 32768 and slots to 1" |
| 11 | `bin/llm-tune doctor` | NOT READY: budget BLOCK (swap free 1G < 4G floor; load 65.65 = 3.65/core > 0.75); fit 133.7 GiB needed vs 107.3 free; lane :8095 alias `qwen3.8-flash-next-tune`; candidate lint clean; 9 probes | works (CLAUDE.md's "doctor" claim true; alias in CLAUDE.md differs: `qwen3.8-flash-next-test`) |
| 12 | `bin/llm-tune plan` | 16 candidates listed (baseline + 15), lint clean | works |
| 13 | `bin/llm-tune budget` | same BLOCK with receipts | works |
| 14 | `bin/llm-tune report` / `best` | 1 trial row `baseline VOID-BUDGET`; `NONE (no BASELINE row)` | works; `logs/tune/ledger.tsv` = 1 data row, `campaigns/20260905-quietbox/trial-baseline/budget-before.txt` |
| 15 | `bin/cleanup.sh` (dry-run default) | only `would truncate logs/v4flash.err.log (17M)`, `logs/flashnext.err.log (17M)`; nothing else | works; but CLAUDE.md:132 "stale root e2e logs (`e2e-*.log`)" — whitelist is 3 exact names, **0 of which exist** |
| 16 | `ls -d sweb-*/` (CLAUDE.md:38 "sweb-*/ MUST stay at root") | `No such file or directory` (0 dirs); 4 unmentioned `sweb-*.log` (952 KB total) | claim stale; cleanup.sh does not cover them |
| 17 | `du -sh .` vs CLAUDE.md:8 "~350GB" and rule 5 "390GB" | **273G** total (models 267G) | stale by 77–117 GB |
| 18 | `bin/gguf-meta.py <shard> --tensor-names --match ple_ngram_embd` + raw header read | shard n_tensors = `0,296,752,175,1` (=1224); match = `1/1224 blk.1.ple_ngram_embd.weight`; `--ctx 262144` → `kv_bytes_total_f16 25,769,803,776` = 24.0 GiB | README:491 "shard 1's header reports 0 tensors" and README:515 "24 KV(f16,262144)" both **verified** |
| 19 | aeon lane (undocumented in CLAUDE.md: `grep -c aeon CLAUDE.md` = **0**) | `bin/serve-aeon-test.sh` exists, `PORT=8095`, `DIR=models/qwen3.8-27b-aeon` which does **not** exist (repo has `models/qwen3.8-27b/Qwen3.8-27B-Q8_0.gguf`) | launcher is dead as written (no `-f` guard: `bin/serve-aeon-test.sh:23` is the raw `exec`) |
| 20 | ledger timestamps / `logs/tune/current` | `logs/tune/current` present; campaigns dir has exactly 1 campaign | consistent with README:506 |

**Incident I must report (my own):** testing item 20-adjacent I ran `PRINT=1 bin/serve-aeon-test.sh` as a dry-run. That script has **no PRINT gate** (`grep -c PRINT` = 0) and no model-existence check, so it `exec`d the real `llama-server`: startup banner observed, process then died on the missing model path. `pgrep -fl llama-server` afterwards shows only the pre-existing swarmlet engine (pid changed 80707→52702 on its own) and ollama's server; `find . -newermt '-5 minutes'` shows **no repo writes**. Nothing is left running, but the dry-run pattern documented for `serve-flashnext.sh` is **not portable**: `PRINT` exists only in `serve-flashnext.sh` (2 refs) and `serve-qwen38.sh` (2 refs); `serve-aeon-test.sh`, `serve-flashnext-test.sh` (inherits), `serve-gemma.sh`, `serve-v4.sh` = 0. Verified `bin/serve-flashnext.sh:82-86` puts `PRINT=1 → print; exit 0` before `exec`, which is why that one is safe.

**Could not test (blocked by the read-only rule, not by the code):** `llm-switch <name>\|off\|restart\|stop\|logs`, `llm-tune baseline\|trial\|campaign\|land\|restore\|note`, `bin/verify-model.sh <port>`, `bin/bench-model.sh <port>`, `run-e2e.sh`/`run-dev*.sh`, `bin/llm-switch-test.sh` (it drives real `launchctl bootout` through stubs), `bin/fetch-model.sh` (downloads).

---

## 2. REQUIREMENTS (step 1)

| Requirement (doc) | Named owner | Enforced by | Challenge |
|---|---|---|---|
| Sole-resident: one big model at a time (CLAUDE.md rule 1) | lotar | **code** — `llm-switch.sh:327` `start_one` only after `stop_one` for all others; OK-reap | KEEP |
| ollama/co-tenants separate — "only ever touch servers serving from **this** repo's `models/`" (rule 2) | lotar (named only by "llm-switch") | **prose only, and false**: `is_llama_cmd` (`:84-105`) matches any `*llama-server*`/`*mlx_lm*` cmdline except ollama paths; `reap_orphans` (`:166-180`) TERM-then-KILLs. `status` today already labels the swarmlet engine on `:8100` (`~/.swarmlet/models/…`) as ORPHAN, and `:8100` is **not** in `IGNORE_PORTS` (`:55`) → any switch/`off` would kill a non-repo server | **DROP the strong sentence or make it true** (add repo-model-path match + `8100`) |
| Never touch in-flight downloads; detection = `llm-switch list` shows on-disk size (rule 3) | lotar | code for downloads (nothing reaps curl); **the detection verb does not exist** | KEEP rule, DROP the sentence |
| KV type per-arch; "gpt-oss MUST keep F16 … GLM/Qwen/Gemma use q8_0" (rule 4) | UNOWNED | prose only | **DROP the arch list** — no gpt-oss/GLM/Gemma weights or lanes exist (`ls models/`, registry = 3 names). Keep one line: "KV is per-arch, flashnext = f16" |
| `models/` sacred (rule 5) | lotar | code — `cleanup.sh` hard whitelist, verified dry-run touched only 2 logs | KEEP (drop the "390GB" number) |
| Tuning owns `:8095`, never `:8099`; `--stop-prod` drains and restores (rule 6) | lotar | **code** — `tune-rig.env` TUNE_PORT=8095; `tune-run.sh:198-252` LANE_IS_PROD guard, in-flight drain, refuses to load while prod listens | KEEP |
| "Names encode the LANE, never the engine" | UNOWNED | prose (serve header:7) + `bin/llm-lanes` | KEEP as one line, single copy |
| Registry→ports snapshot "2026-08-27 … PROD" | UNOWNED | code (`port_of`) | **DROP the dated snapshot** (code table + `llm-lanes` is the truth; script header itself says ports retired 2026-09-15) |
| "Co-tenants (`:8112`/`:8116` mlx, ollama) never reaped" | lotar | code, but only via a hardcoded `IGNORE_PORTS` default | KEEP; make it robust (see SIMPLIFIED) |
| Directory structure "keep it this way" (10 rows) | UNOWNED | nothing (6 rows name non-existent files) | **DROP the tree** |
| "MUST stay at root: `openai__glm-air.*.json`, `sweb-*/`" | SWE-bench harness author (scripts still at root) | `run-dev.sh:10,16,20` hardcode root paths | KEEP for the 3 reports; DROP `sweb-*/` (0 dirs) |
| "Working tree lives in a Claude job tmp — copy to `evals/` before the job is deleted" | UNOWNED | done: tmp gone, `evals/` holds 78 entries + README/STATUS | **DROP** → pointer to `evals/STATUS.md` |
| Rule of thumb "5 blind rounds, 50–82% solvable" | UNOWNED | none | **DROP from CLAUDE.md** (duplicate of `evals/STATUS.md`, which has the receipts) |
| README "Current Flash-Next status (verified 2026-09-09)" | claude (audit author) + lotar | date-stamped evidence | KEEP — this is the only canonical current section |
| README "Historical catalog" (4872 w = 96.3% of README) | UNOWNED | none | KEEP as receipts, relocate (see SIMPLIFIED) |
| `plans/eval100-plan` ("status: IN PROGRESS 2026-09-05") | claude (author), lotar (pending user decision) | none | Archive: references a deleted jobs dir, a missing `bin/serve-v4-eval.sh`, and `:8100` which the mesh now owns |
| `plans/HUD_TASK_CONTRACT_v2` | external SWE-bench reviewer (accepted 12-round vetting) | referenced by 6 `evals/*` files | KEEP |
| "fleet/gbrain now use the swarmlet mesh node on `:47800`" (in-repo quote, script header:13) | UNOWNED | none | **Dated assertion about another service** — per parent steering the mesh is unstable; make it a dated observation, not a fact |

---

## 3. DELETED (step 2) — proposed removals, not applied

| # | Item | Class | Size | Proof nothing is missing with it | Risk if I'm wrong | Restore |
|---|---|---|---|---|---|---|
| D1 | CLAUDE.md model-presence paragraph (lines 127-129) | **A** | ~70 w | `ls models/` (6 dirs), `du -sh models/*`, `bin/serve-aeon-test.sh`, eval100-plan:24 | loses "v4flash is NOT re-downloadable" → keep that clause (~10% add-back) | git |
| D2 | CLAUDE.md verbs `list` / `restart` / `stop` / `logs` + rule-3 detection sentence | **A** | ~5 lines | executed `list` → usage; `llm-switch.sh:367-388` cases | users lose a size check → point at `du -sh models/*` (exists) | git |
| D3 | 6 tree rows for non-existent files (`fetch-glm.sh`, `serve.sh`, `bin/bench.sh`, `bench_refine.sh`, `head2head.sh`, `lossless_check.sh`) | **A** | 6 lines | `test -e` each = MISSING; only refs are the docs themselves | none | git |
| D4 | CLAUDE.md Evals bullets: job-tmp instruction + rule-of-thumb paragraph | **A** | ~50 w | `test -e ~/.claude/jobs/d02c4b1e` = MISSING; `evals/STATUS.md` holds the numbers | loses the 50–82% heuristic in the agent-facing file | git |
| D5 | CLAUDE.md "`sweb-*/` MUST stay at root" + "cleanup removes stale root e2e logs (`e2e-*.log`)" | **A** | 2 lines | `ls -d sweb-*/` → 0; `cleanup.sh:43` lists 3 exact names, 0 present | someone deletes the 3 `openai__glm-air.*.json` (they stay in the paragraph) | git |
| D6 | numbers "390GB" / "~350GB" | **A** | 2 tokens | `du -sh .` = 273G | none | git |
| D7 | rule 4 gpt-oss/GLM/Gemma arch list | **A** | 2 lines | `ls models/` = 0 gpt-oss/GLM/Gemma; registry = 3 names | the F16-KV lesson vanishes → keep as "KV dtype is per-arch; flashnext=f16" | git |
| D8 | README "predict 32768" (line 370) | **A** | 1 token | `PRINT=1 serve-flashnext.sh` → `--predict 52428` | none (serve script is canonical) | git |
| D9 | README "ollama :53176" (line 519) and "llm-switch owns …/ds4vision" (line 517) | **A** | 2 tokens | ollama's llama-server is on `59883`; `serve-ds4vision.sh` MISSING + script header says removed 2026-09-11 | none | git |
| D10 | 4 root `sweb-*.log` (952 KB) | **B** | 952 KB | `grep -rl` → NONE references them | they are `*.log` → **gitignored, unrecoverable**; dated Jun-26 SWE-bench evidence | move to `logs/archive/`, don't `rm` |
| D11 | `plans/eval100-plan-20260902.md` in `plans/` | **B** | 37.7 KB / 5101 w | superseded by `evals/STATUS.md` (Sep 5 18:14, newer) | loses the 5-lane model table used by the eval programme | move to `docs/archive/` |
| D12 | llm-switch.sh header block lines 9-30 (41-line comment) | **B** | ~22 lines | duplicates CLAUDE.md+README; already carries stale "128K ctx, 32K out" vs actual 262144/52428 | loses the `LLM_SWITCH_*` env-var contract (exists nowhere else) → keep those 6 lines | git |

Expected add-back ~10%: the v4flash re-downloadability warning, the `LLM_SWITCH_*` env contract, the F16-KV lesson — all names above, keep-conditionally.

---

## 4. SIMPLIFIED (survivors only)

1. **One canonical current-state surface.** CLAUDE.md carries **rules, commands, pointers only**; zero ctx/port/quant/size facts. Live facts come from `bin/llm-lanes` (ports), `PRINT=1 bin/serve-<lane>.sh` (argv), `bin/llm-tune doctor` (lane+ctx+kv+budget). README lines 1-20 stays the single dated narrative; the 4872-word catalog moves to `docs/history/` with README keeping a pointer (its receipts — measured t/s, MTP audit — are the expensive part, so relocation beats deletion).
2. **Rule 2 rewritten to match code, then made true.** Either say "llm-switch reaps any *llama-server*/mlx_lm listener not in IGNORE_PORTS" (honest) or add a `--model` path check for `$RIG_REPO/models`. Current text is a safety promise the code does not keep (see D-hazard below).
3. **Rule 4 → one line** ("KV dtype is per-arch; never copy samplers/KV recipes across models; flashnext=f16, set in the serve script") — the per-arch table belongs in the archived catalog, not in the agent's always-loaded file.
4. **`IGNORE_PORTS` hardcoded list → derived.** `8111 8112 8114 8115 8116 11434` is a snapshot of other services' ports; it will silently rot (8112/8116 are up right now and are the only thing protecting them). Derive from "listener whose model path is outside this repo" — that is the rule the doc already claims.
5. **Duplicate-fact ranking (one canonical copy each).** ports/ctx/kv → `bin/llm-lanes` + serve script (execute, never transcribe). Registry incl. retirement notes → `llm-switch.sh` header. Model catalog + measurements → `README.md` §Current + `docs/history/`. Tuning rules → `bin/tune-rig.env` + skill usage text. Eval status → `evals/STATUS.md`. CLAUDE.md keeps only what none of those can express: the refusal rules (sole-resident, one big model, tuning≠prod, don't touch downloads) and pointers.
6. **`PLANS.md`-style index** (one screen: file → what it is → status → superseded-by) so `plans/` and `docs/` stop accumulating status lines that go stale inside the file.

---

## 5. ACCELERATED (step 4)

None on the docs themselves — pruning was the win. One cheap cycle-time item: the two ops commands already run per session (`llm-switch status`, `llm-tune doctor`) are the right place to surface doc drift, so the cost of noticing staleness drops from "an auditor re-reads 12k words" to "one line in a command the operator already runs".

---

## 6. AUTOMATED (step 5)

| Automate now (read-only, no lane) | Command shape | Why safe |
|---|---|---|
| Doc-claim checker | `du -sh models/*` · `bin/llm-lanes` · `PRINT=1 bin/serve-<lane>.sh` parse · `test -e` on every path named in CLAUDE.md/README · `curl -m2 /health` for each registry port | all read-only; today it would have caught 10 dead pointers, 4 phantom verbs, the 273G/390G drift, the `predict` drift |
| `llm-tune doctor` verdict line into the same output | already exists | shows lane/ctx/kv without prose |
| Stale-status lint for `plans/` + `evals/STATUS.md` (mtime > 14 d and status `IN PROGRESS`) | `find plans evals -name '*.md' -newermt …` | flags eval100-plan's 11-day-old IN PROGRESS |

Must wait: everything requiring a loaded model (`verify-model.sh`, `bench-model.sh`, `llm-tune baseline/trial`, `run-e2e.sh`). Automating them today would cement a harness whose budget gate blocks on this box anyway (doctor: NOT READY, 3 BLOCK lines).

---

## 7. SCOPE

**My lane:** rewrite CLAUDE.md's machine facts as pointers, delete D1–D9, move D10/D11 as documented, and hand off: (a) the ORPHAN-reap hazard (`8100`, `IGNORE_PORTS`, `is_llama_cmd`) to whoever owns `bin/llm-switch.sh`, (b) `PRINT` only existing in 2 of 6 serve scripts to whoever owns `bin/serve-*.sh`, (c) the 4 root `sweb-*.log` to whoever owns repo hygiene. Proposal only (out of lane): a `bin/llm-doccheck` read-only verb.

---

## 8. BLOCKERS

| Not verified | What I would need |
|---|---|
| Every lane-dependent claim (`verify-model.sh`, `bench-model.sh`, the documented t/s rows, README:472-478 measurements) | permission to load one lane — **not requested**, conflicts with live-inference safety |
| The 6 blocked verbs' runtime behaviour (`baseline/trial/campaign/land/restore/note`, `llm-switch <name>\|off`) | acceptance of a stop-prod window; out of scope for a read-only audit |
| gbrain / fleet / mesh pointers (`47800`, "fleet/gbrain point at :8099") | read-only mesh/gbrain access I deliberately did not take; and per parent steering the mesh re-places deployments ≈ every 11 min, so **no single observation is ground truth** — any doc sentence asserting mesh stability or "fleet points at :8099" is falsifiable today and should carry a date + an explicit "verify with `<cmd>`" instead of a fact |
| `logs/flashnext-mtp-ab-20260909T102100Z/REPORT.md` internals | out of my lane's reading budget; the README pointer to it is live and the file exists |
