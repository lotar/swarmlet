# L3 — gbrain ingest hooks (`/Users/lotar/projects/ai-fleet/fleet/hooks`, 17 files / 4,060 lines)

Auditor: L3 scout. Window: 2026-09-16 10:43–11:05 +0200. Read-only: no hook was executed beyond `py_compile`;
no server/model/lock/DB write was issued by any command below except the two side effects disclosed in E2E-12.

---

## 1. E2E CHECK

### E2E-1 — compile sweep (brief-mandated, executed)
```
cd /Users/lotar/projects/ai-fleet/fleet/hooks
for f in *.py; do python3 -m py_compile "$f" && echo "OK $f" || echo "COMPILE FAIL $f"; done
```
Observed: `OK` × 11 (`gbrain-ingest.py gbrain-session-sweeper.py gbrain-stop-adapter.py gbrain_session_format.py gbrain-consult.py skill-router.py skill-contract-stop.py read-budget-guard.py edit-before-repro-guard.py codex-foreground-guard.py fleet-agent-registry.py`). Verdict: **works**.

### E2E-2 — "does each hook declare its interface?" (static, not executed)
`for f in *.py; do python3 "$f" --help; done` was **not run** (parent steering: hooks without a documented
dry-run must be traced, not executed). Static result:
```
grep -Ln "argparse" *.py   -> 11/11 files: no argparse anywhere
grep -n "sys.argv" *.py    -> only 2 hits: gbrain-ingest.py (--child), gbrain-stop-adapter.py (harness arg, --child)
grep -n "sys.stdin" *.py   -> 9/11 files
```
Verdict: **no hook declares a CLI interface**; every one is a stdin-protocol hook and its only contract is its
module docstring. `gbrain-session-sweeper.py` takes no arguments at all (`def main()` at L1894, no argv read) →
running it executes all 9 lanes and writes to the brain + Postgres. It must never be invoked for a test.

### E2E-3 — is there a rehearsal mode? (grep, executed)
```
grep -n "dry\|DRY" *.py *.sh opencode/*.ts pi/*.ts
```
Observed: 1 hit, unrelated (`git-pre-push.sh:18` mentions another subsystem's `FLEET_DRYRUN`). Verdict: **no dry-run, no `--once`, no `--print` in any of the 17 files**; the sweeper has no argv handling whatsoever.

### E2E-4 — write-path trace (the substitute for executing; executed parses + code read)
| File | Write reached by a hook run | Line |
|---|---|---|
| gbrain-ingest.py | `subprocess.run([GBRAIN_BIN,'put',slug,'--content',content])` | L150-153 (inside `run_child`, called only from `--child`) |
| gbrain-ingest.py | log append | L38-43 |
| gbrain-stop-adapter.py | `subprocess.run([GBRAIN_BIN,'put',…])` | L327-330 |
| gbrain-stop-adapter.py | state.json rewrite under sweeper.lock (`os.replace`) | L266-280 |
| gbrain-stop-adapter.py | log append | L58-63 |
| gbrain-session-sweeper.py | `gbrain_put` → `subprocess.run(gbrain put)` | L192-211 (L201) |
| gbrain-session-sweeper.py | `run_import_dir` → `subprocess.run(gbrain import …, env=GBRAIN_PAGE_BLOCK_BYTES)` | L1246-1264 |
| gbrain-session-sweeper.py | `_psql` → `docker exec gbrain-postgres psql` (13 call sites: source create/archive/unarchive L1195/L1209/L1221, title UPDATE L1574, timeline INSERT L1610-1619, links INSERT L1757-1765, reads L1167/L1178/L1524/L1543/L1652/L1675/L1706) | L1153-1159 |
| gbrain-session-sweeper.py | state.json / log | L169-176 / L107-113 |
| gbrain-consult.py | no file write at all; one **POST** to the local broker `http://127.0.0.1:7077/search` (urllib `Request(..., data=...)`, a search query — not a brain write) | L87-95 |

My commands reached none of these: the only lane code I executed was `py_compile` (compiles, never imports)
and my own `/tmp/e2e-audit/L3/e2e_parity.py`, which imports the modules (module bodies are constants only,
`__name__ != "__main__"`) and calls pure functions (`parse_*`, `render_page`, `bounded_conversation`,
`discover_*`, `newest_md_mtime*`, `iso_to_epoch`). `main()`, `run_child()`, `ingest_one()`, `process_*`,
`gbrain_put()` and `_psql()` were never called. Proof by absence: `state.json` mtime/size unchanged
(10:49:00 / 300232 B), no `sweeper.lock` left, and `grep -c e2e-audit` = 0 in `gbrain-sweeper.log` and
`gbrain-ingest.log` (1 in `gbrain-stop-adapter.log` — see E2E-12).
Verdict: **works (traced, not executed)**.

### E2E-5 — parse+render parity on real transcripts (executed, `/tmp/e2e-audit/L3/e2e_parity.py`)
```
PYTHONDONTWRITEBYTECODE=1 python3 /tmp/e2e-audit/L3/e2e_parity.py
```
Fixtures copied read-only into `/tmp/e2e-audit/L3/fixtures/` from `~/.codex/sessions/2026/09/15/rollout-…jsonl`
(180 KB), `~/.grok/sessions/…/wave8-B/01a0a039-22b5-…` (1.29 MB), `~/.pi/agent/sessions/--Users-lotar--/…jsonl`.
| lane | sweeper parse | adapter parse | slug | page bytes |
|---|---|---|---|---|
| codex | sid `01a0a477-85a9-7fc3-…`, 1 msg | identical sid, 1 msg | `session-codex-01a0a477-85a9-7fc3-bcc3-744b99a7c3cb-20260915-1148` for both | 1,087 = 1,087, **byte-identical** |
| grok | sid `01a0a039-22b5-…`, 9 msgs | identical sid, 9 msgs | `session-grok-01a0a039-…-20260914-1601` for both | 4,795 = 4,795, **byte-identical** |
The two writers genuinely read the same shape and produce the same slug and the same bytes.
Verdict: **works** — the shared-slug contract holds live, not just in intent.

### E2E-6 — formatter invariants (executed)
`session_budget()=40000`; 63 rendered messages → `body=38282 ≤ 40000`, `captured=19/63`; envelope +
gbrain-block stripping → `[('user','real question'),('assistant','answer')]`; title of
`"## Heading\n\n  Fix the **ingest** [hook](http://x)"` → `'Fix the ingest hook'`. Verdict: **works**.

### E2E-7 — slug-construction lines (the four-defect verification, all four present)
```
hooks/gbrain-ingest.py:148        slug = f"session-{session_id}-{time.strftime('%Y%m%d-%H%M', time.localtime(created_epoch))}"  # FULL id
hooks/gbrain-stop-adapter.py:325  slug = f"session-{harness}-{str(session_id)}-{ts}"  # FULL id: truncation collided time-ordered ids
hooks/gbrain-session-sweeper.py:1862  slug = f"session-{harness}-{str(session_id)}-{ts}"
git show d439638 -- hooks/*.py    -> -  session-{session_id[:12]}-  / +  session-{session_id}-   (trio fixed in one commit, 2026-09-15)
```
Verdict: **fix present in all three builders** (but note: the stop-adapter docstring L19-21 still says
`slug is session-<harness>-<sid12>-<created ts>` — stale doc, code is full-id).

### E2E-8 — coverage of the three trees (code + live discovery, executed)
| tree | code | live measurement |
|---|---|---|
| harness session dirs | `scan_codex` L278, `scan_grok` L385, `scan_opencode` L496, `scan_pi` L603, `scan_pi_subagent` L742 (`PI_SESSIONS_GLOB` L547, `PI_SUBAGENT_ARTIFACTS_GLOB` L648); plus the two Stop hooks | files on disk → pages: claude 11,455 → 10,586; codex 7,428 → 4,797; grok 424 dirs → 1,576 pages; pi 365 → 211; pi-subagent 214 → 195 |
| memory dirs | `discover_memory_dirs` L1086 + `process_memory_lane` L1313 (`~/.claude/projects/*/memory`) | 317 dirs discovered, **18** have a top-level `.md`, 17 settled, `MEMORY_PER_RUN_CAP=3` → 1.4 h to drain |
| job tmp markdown | `process_jobs_md_lane` L1438 + `discover_job_md_dirs` L1408 + `JOBS_MD_SOURCE_ID="claude-jobs"` L1395 | 14 `jobs/*/tmp` dirs, 6 hold `.md`; last tick `imported=7 skipped=1601 errors=0 chunks=164` (10:48:31) |
Verdict (harness lane): **partially working** — 2,625 codex rollouts are settled (>48 h) and have **no** page;
the scan floor is the watermark, so anything that settled before a lane's first 48 h window is unreachable forever.
Verdict (memory + jobs-md): **works as designed**.

### E2E-9 — `GBRAIN_PAGE_BLOCK_BYTES` honouring (traced, one-hop beyond the lane)
`gbrain-session-sweeper.py:1405 JOBS_MD_PAGE_BLOCK_BYTES="2000000"` → `L1467-1468 run_import_dir(..., extra_env={...})`
→ `L1254-1256 env.update(extra_env)` → `L1262 subprocess.run([... gbrain import ...], env=env)` →
CLI `~/.bun/install/global/node_modules/gbrain/src/core/config.ts:570-571` (`process.env.GBRAIN_PAGE_BLOCK_BYTES`),
default `DEFAULT_BYTES_BLOCK = 500_000` (`core/content-sanity.ts:66`). Load-bearing today: exactly **4** files sit in
the 500 KB–2 MB band (838,543 / 748,745 / 666,437 / 624,611 B), **0** above 2 MB. Verdict: **works**, and 2 MB is 2.4×
the largest real file — the ceiling is 2.4× looser than it needs to be.

### E2E-10 — embedding identity (DB truth, read-only SELECT)
```
docker exec gbrain-postgres psql -U gbrain -d gbrain -tAc "SELECT model,count(*) FROM content_chunks GROUP BY 1"
  -> ollama:nomic-embed-text:768|80053            (1 distinct value)
SELECT embedding_signature,count(*) FROM pages WHERE deleted_at IS NULL GROUP BY 1
  -> ollama:nomic-embed-text:768|24430 ; NULL|74
pages whose signature <> their chunks' model -> 0
```
The fix does **not** live in this lane: it is in the gbrain CLI package
(`src/commands/embed.ts:601`, `src/core/import-file.ts:822,1182` stamp `currentEmbeddingSignature()`,
defined `src/core/embedding.ts:178`). The hooks' only contribution is the `GBRAIN_SOURCE=default` pin
(`gbrain-ingest.py:184`, `gbrain-stop-adapter.py:368`). Verdict: **works, but out of lane** (see SCOPE).

### E2E-11 — DB truth on the collision legacy (read-only SELECT)
| slug class | pages |
|---|---|
| 12-char truncated sid, harness-tagged (`session-(codex\|grok\|opencode\|pi)-<sid[:12]>-<ts>`) | 6,725 |
| 12-char truncated sid, claude-style (`session-<sid[:12]>-<ts>`) | 10,584 |
| full UUID, claude-style | **2** |
| full UUID, codex-ish | 335 |
| opencode full (`ses_…`) | 247 |
| pi-subagent full | 119 |
**95.7 % of the 18,091 session pages (17,309) still carry a pre-fix truncated slug.** One legacy family has
**119 pages for a single codex session** (`session-codex-019f4bd1-88f-20260710-1337 … -1515`, one rollout file
exists on disk). Duplicate weight: 573 slug-bases hold >1 page; rank≥2 pages = **1,031 pages / 11.87 MB** of the
68.50 MB of session page text (17.3 %). Post-fix duplicate pairs already exist:
`session-4f7b4b3a-b3c-…` **and** `session-4f7b4b3a-b3cf-4776-…` (same session, two slugs).
Verdict: **the fix is correct but 95.7 % of the damage is still in the table**, and the fixed slugs *create* one
duplicate pair per session that was previously ingested under the truncated slug.
Live corroboration from the hook's own log (`~/.ai-fleet/logs/gbrain-ingest.log`):
`10:43:14 session=4f7b4b3a-… put OK slug=session-4f7b4b3a-…-20260907-1242 chars=39363 msgs=63/959` and
`10:57:04 … same slug … chars=39245 msgs=61/963` → the Stop hook re-renders the whole transcript and overwrites
the same page; a later render can carry **fewer** messages (63→61), i.e. last-writer-wins also applies post-fix.

### E2E-12 — side effects I caused (disclosure)
1. `py_compile` refreshed **14** `.pyc` files in `fleet/hooks/__pycache__/` (~230 KB, gitignored — `git status --short hooks/` is clean). Brief-mandated command.
2. **One** line was appended to `~/.ai-fleet/logs/gbrain-stop-adapter.log` at `10:58:24` by
   `adapter.parse_grok("/tmp/e2e-audit/L3/fixtures/summary.json")` — my first (mis-laid-out) fixture made the
   parser's own `log()` fire. `grep -c e2e-audit gbrain-stop-adapter.log` = **1**, sweeper/ingest logs = 0.
   Lesson for the lane: **its parsers are not side-effect-free** — `parse_grok`/`parse_codex` call `log()` on
   read failure, so "just parsing a file" writes under `~/.ai-fleet`. No brain write, no state write.

### E2E-13 — live-system evidence (no command of mine involved)
Sweeper ticks every 900 s (`com.fleet.gbrain-sweeper.plist`, StartInterval 900): `10:08 → 10:23 → 10:38 → 10:53`,
`run complete: 0-1 put(s)`, last tick `7 timeline backfilled, 7 orphan(s) in 1 bucket(s), 2 hub(s) written,
7 link(s) added`. `PER_RUN_PUT_CAP` was hit **35** times in 50,861 log lines, the last on 2026-09-15 01:03.
Verdict: **works, steady state = 0–1 put/tick; the cap is a burst guard, not a chronic-bug mask.**

---

## 2. REQUIREMENTS (step 1)

| # | Requirement | Named owner | Verdict |
|---|---|---|---|
| R1 | Every agent session on this Mac ends up as one searchable brain page (5 harness session lanes + 2 Stop hooks) | Mladen Lotar `<mladen@the-shop.io>` (author of 23/30 hooks commits) | **keep** — this is the lane's purpose; measured retrieval: 5,141/18,091 session pages have `last_retrieved_at` |
| R2 | Slug = full session id (no `[:12]`) | Mladen Lotar (fix commit `d439638`, 2026-09-15) | **keep** — verified by executed parity (E2E-5); truncation was silent data loss |
| R3 | The two writers of one session emit byte-identical pages | Mladen Lotar (`gbrain_session_format.py` spec v2) | **keep** — verified byte-identical, 1,087 B and 4,795 B |
| R4 | Page body ≤ `GBRAIN_SESSION_BUDGET` (40 KB), 2 KB/message | Mladen Lotar (D4) | **keep** — evaluated `38282 ≤ 40000`; caps the 119-page family at 40,151 B/page |
| R5 | `MIN_CONTENT_LEN=80` skip | **UNOWNED** (copied comment `gbrain-ingest.py:22`) | **drop** — redundant with the `total == 0` check above it (a page with ≥1 message is always > 80 chars: the header alone is ~100). Evidence: `grep -c "content too short" gbrain-ingest.log` = **14 lifetime hits, last 2026-07-02T14:28:31**, i.e. the guard has not fired once in the 4.5 MB of log written since, while `metadata-only` fired 2,015 times |
| R6 | `PER_RUN_PUT_CAP=40` shared across 5 session lanes + 2 report/job lanes | **UNOWNED** | **keep**, but de-couple (see SIMPLIFIED S6) — hit 35× in 50,861 lines, last 2026-09-15, so it does not currently starve anyone |
| R7 | `MAX_IDS_PER_HARNESS=2000` id retention | **UNOWNED** | **drop to 200** — see DELETED D1; 88 %/96 % of retained ids are >7 d old and unreachable |
| R8 | Memory-dir lane, 3 imports/run, 5-min settle | Mladen Lotar (lane-4 comment L78-84) | **keep** — 17 dirs due, drains in 1.4 h |
| R9 | jobs-md lane + `GBRAIN_PAGE_BLOCK_BYTES=2000000` | Mladen Lotar (commit `64912f7`) | **keep**, tighten ceiling to 900 KB (D6) |
| R10 | pi-subagent-report lane (517 pages from 765 files) | Mladen Lotar | **keep** — 38/517 ever retrieved |
| R11 | title janitor, 20 retitles/run | **UNOWNED** | **keep** — 0 blob titles remain in 18,091 session pages; it already won |
| R12 | graph janitor: "every live page carries ≥1 link + ≥1 timeline entry" (brain_score 100/100) | **UNOWNED** — no human asked for the score; the requirement is "keep `gbrain doctor` green", cited in commit `d439638` | **challenge (B)** — it manufactures **23,006 of the 27,515 links (83.6 %)** and **22,158** synthetic `heal-backfill` timeline rows; 116/205 hub pages have ever been retrieved, so it is not provably dead. Owner must answer: who consumes `hubs/*`? |
| R13 | `~/.claude/jobs/<id>/state.json` snapshot page per job (6 pages) | Mladen Lotar (L944-963) | **keep** — 3/6 ever retrieved, cheap |
| R14 | 7 non-gbrain guard hooks in the same dir (codex-foreground, read-budget, edit-before-repro, skill-contract-stop, todo-goal-gate, tracker-shrink-guard, git-pre-push) | Mladen Lotar (file-level last authors) | **3 drop, 4 keep-if-fleet-tick** — see DELETED D2/D4 |

---

## 3. DELETED (step 2) — removal candidates, all classed, none executed

| # | Item (exact path) | Class | Size | Evidence command → result | Risk if wrong | Restore |
|---|---|---|---|---|---|---|
| D1 | `MAX_IDS_PER_HARNESS` 2000 → 200 (`gbrain-session-sweeper.py:76`) | **B** | saves **98,080 B = 33 %** of `state.json` (codex 80,000 B/2000 ids, grok 58,080 B/1452) | `python3` decode of the ids as UUIDv7 → codex 1,768/2,000 ids >7 d, grok 1,391/1,450 >7 d; `scan_codex/scan_grok/scan_pi` on the live state → 1 candidate/15 min, already in `ingested_ids`, and the scan floor is the watermark (`L280,387,498,605,744`) so older ids can never be candidates again | a *resumed* old session re-puts → same slug (created comes from the FIRST `session_meta`, D6) = **idempotent update**, not a duplicate. Only the mtime-fallback path (grok summaries lacking `created_at`) can fork a slug — measured **0/400** grok summaries lack `created_at` | bump the constant; one line |
| D2 | `hooks/skill-contract-stop.py`, `hooks/read-budget-guard.py`, `hooks/edit-before-repro-guard.py` | **B** | 10,968 + 4,341 + 4,368 = **19,677 B / 566 lines** | `grep -rl` over the live configs (E2E-2 table) → no hit in `~/.claude/settings.json`, `~/.claude/settings.local.json`, `~/.codex/config.toml`, `~/.grok/hooks/*.json`, `~/.config/opencode/opencode.json`, `~/.pi/agent/settings.json`, `~/Library/LaunchAgents/*.plist`; only `fleet/config/skill-gate.settings.fragment.json` (a paste-me fragment, `_comment` says "MERGE TARGET") + tests. `fleet/config/fleet-hooks.settings.json` has **no** `Stop` array and **no** `Read` matcher → the merge never happened | they are the skill-gate experiment; 3 files of unlanded work | `git -C fleet checkout -- hooks/<file>`; both commits `99fedca`/`fb18ffd` are in the fleet submodule |
| D3 | Legacy duplicate pages (rank≥2 of each slug base) | **B** | **1,031 pages / 11.87 MB** text + their chunks | SQL in E2E-11; family sample `session-codex-019f4bd1-88f-20260710-*` = 119 pages for one session | destructive; needs an owner decision + a re-ingest plan | `~/.ai-fleet/backups/pg/gbrain-2026-09-16-0230.sql.gz` (773 MB, `dump OK 02:32`, 8 kept / 7-day retention); re-ingest is cheaper — the source rollouts still exist |
| D4 | `hooks/git-pre-push.sh` | **B** | 3,288 B / 65 lines | loop over `~/projects/*/.git/hooks/pre-push` → **0 installed**; the installer `fleet/bin/install-git-hooks.sh` was never run in this checkout (`.git/hooks/` has only `*.sample`) | it is referenced by the fleet repo's own tests (`tests/validate.sh:…`, `test_install_git_hooks.sh`) → deleting breaks those tests | `git -C fleet checkout -- hooks/git-pre-push.sh` |
| D5 | `JOBS_MD_PAGE_BLOCK_BYTES` `"2000000"` → `"900000"` (`gbrain-session-sweeper.py:1405`) | **B** | 0 bytes today (config value) | `find ~/.claude/jobs -name '*.md' -size +500k` → 4 files (max 838,543 B); `-size +2048k` → **0** | a future >900 KB job export lands page-only instead of chunked — which is the rule the comment already claims ("must not chunk an unbounded blob") | revert one literal |
| D6 | Stale docstring claim in `hooks/gbrain-stop-adapter.py:19-21` ("slug is …sid12…") | **A** | 3 lines | `sed -n '19,21p'` vs `L325` (full id) | none (doc only) | revert one line |
| D7 | `title_janitor_candidates` can never match a claude page (candidate SQL `L1518` matches `^session-(codex\|grok\|opencode)-`, `title_janitor_kind` L1495 has no claude branch; claude titles are `"<snippet> (claude session YYYY-MM-DD)"`) | **B**, latent | 0 pages today | `SELECT … count(*) FILTER (WHERE title ~ blob-patterns)` → **0/18,091** blob titles right now | a claude page whose snippet is an unparseable blob stays permanently untitled | add one `OR slug ~ '^session-[0-9a-f]{8}-'` / one `kind` branch |

**~10 % I expect to come back:** the 3 skill-gate hooks (D2) if the fragment is merged late; part of the id
retention (D1) if a resumed-session storm appears; and D5 if a job export legitimately needs 1 MB chunks.

---

## 4. SIMPLIFIED (step 3) — survivors only

| # | Simplification | Measured duplication / cost |
|---|---|---|
| S1 | One shared `gbrain_harness_parse.py` for codex+grok (the two files read the *same* shapes — proved by E2E-5) | `parse_codex_file` 47 L vs `parse_codex` 51 L share **26/49** statements (0.56); `parse_grok_session` 55 L vs `parse_grok` 55 L share **35/53** (0.62) → **61 duplicated statements** |
| S2 | Move `log()`, `iso_to_epoch()`, `is_pid_alive()`, `acquire_lock()`, `release_lock()` into that module | **45 further duplicated statements** (ratios 0.86 / 0.75 / 1.00 / 0.78 / 1.00). Total duplication ≈ **106 statements ≈ 140 lines** out of 2,333 in the two files |
| S3 | Move the adapter's `mark_ingested` (37 L, incl. 4× retry loop with `sleep(2)` = up to 8 s) onto the sweeper's state helpers (`save_state` L169 is 8 L) | removes a 2nd writer of `state.json` under the same lock |
| S4 | Replace the 3 hand-copied host files with symlinks (precedent already in the tree: `~/.config/opencode/plugins/agent-bus.ts` **is** a symlink) | 39,308 B of copies that are byte-identical today (verified) and drift silently tomorrow; **no installer exists** (grep for `config/opencode/plugins\|pi/agent/extensions` in `bin/`,`fleet/bin/`,`adapters/` → 0 hits) |
| S5 | Drop the harness-less claude slug shape — make it `session-claude-<uuid>-<ts>` like the other four | today `session-claude-%` = **0 rows** while 10,586 claude pages exist under `session-<uuid>-<ts>`; every prefix-based classifier (incl. `title_janitor`, D7) silently misses the largest lane |
| S6 | Make `PER_RUN_PUT_CAP` per-lane or make the put order round-robin | one shared counter with 7 lanes in fixed order means a codex backlog starves pi/reports in the same tick |
| S7 | `PI_REPORT_MAX_CHARS=200000` — the body travels on `gbrain put`'s argv (`L794` comment: "far under ARG_MAX", macOS `ARG_MAX` = 1 MB), and the lane's own `PI_REPORT_MIN_CHARS=200` filter means the cap only ever bites on a >200 KB report (none observed in 765 files). Keep | no change |

---

## 5. ACCELERATED (step 4)

| # | Win | Measured |
|---|---|---|
| A1 | `_psql` spawns `docker exec` per call: 13 call sites, ≈10 executed per tick | **228 ms/call**; batching each lane's statements into one `docker exec … psql` saves ≈ **2 s/tick**. (Using the published port `127.0.0.1:55432` would be faster still but `psql` is **not installed on the host** — `which psql` → not found.) |
| A2 | Memory lane walks 317 dirs to find 18 with markdown | `os.scandir` pre-filter removes 299 no-op walks/run |
| A3 | Stop hook hot path — **already cheap, do not touch** | python3+imports **70 ms**; child parse **7 ms** for a 1.29 MB grok transcript, **2 ms** for 180 KB codex; state.json parse+dump **2 ms**; the only unbounded wait is the lock retry (≤8 s) which runs in the detached child, never the hook's 10–30 s budget |
| A4 | Sweeper's `pi_report_existing_pages()` pulls 517 slugs every run to skip re-puts | keep (one query, 228 ms) |

---

## 6. AUTOMATED (step 5, last)

| # | Automate now | Why it is safe |
|---|---|---|
| Au1 | Nightly **read-only** duplicate/legacy auditor: one SELECT that counts truncated-slug pages + rank≥2 pages, appended to `~/.ai-fleet/logs/` | no writes; it is the measurement D3 needs before any destructive change |
| Au2 | An installer that symlinks the 3 host copies (`~/.config/opencode/plugins/gbrain-bridge.ts`, `…/fleet-skill-gates.ts`, `~/.pi/agent/extensions/skill-router.ts`) → repo files, mirroring `install-git-hooks.sh`'s "symlink, never copy" rule | reversible, removes a drift class; the precedent file already is a symlink |
| **must wait** | D1 (cap 2000→200) until the re-put rate is measured: count `put OK` lines whose slug already existed with an older `updated:` header | without that count, "idempotent update" is reasoning, not evidence |
| **must wait** | D3 (delete 1,031 duplicate pages) until an owner signs and the 7-day dump window is confirmed | destructive, brain-wide |
| **never** | Do not add a new cron lane for the graph/title janitor, and do not extend the sweeper's 9 lanes, before R12 is owned | automating a brain_score chore first is precisely what step 5 forbids |

---

## 7. SCOPE

Lane L3 = the 17 hook files; its defensible core is R1–R4 + R8–R10 (session → searchable page, three trees,
byte-identical writers). **Proposals outside the scrubbed scope, in priority order:**
1. `bin/gbrain-heal.sh` + the gbrain CLI's `node_modules` patch guard own the embedding-identity fix (E2E-10) — that belongs to the gbrain-CLI lane, not L3.
2. The 2,625 settled codex rollouts with no page (E2E-8) need a one-off backfill decision (watermark is a one-way ratchet).
3. `MAX_IDS_PER_HARNESS` (D1) is the highest-value single-line change in the lane (33 % of state.json).
4. 7 of the 17 files implement non-gbrain guards (codex-foreground / read-budget / edit-before-repro / skill-contract-stop / todo-goal-gate / tracker-shrink-guard / git-pre-push) — if this lane is meant to be "the gbrain ingest hooks", 3 are unlanded (D2) and 4 are fleet-tick-only.

## 8. BLOCKERS

| Blocker | What I would need |
|---|---|
| No hook could be run end-to-end (no dry-run anywhere; steering forbids executing them) | an `--once --dry-run` flag on the sweeper (print candidate slugs + byte counts, no `put`) — then the put latency and per-tick wall time become measurable |
| `gbrain put` latency unmeasured (would need to execute the CLI) | `time ~/.local/bin/gbrain --version` or a `--dry-run` put mode; I chose not to run the CLI |
| Whether anything consumes the janitor's 23,006 hub-index links / 22,158 `heal-backfill` timeline rows | read `bin/gbrain-broker.py` + the CLI search path (outside lane) or an owner answer to R12 |
| Hash of the patch guard for the embedding fix | `bin/gbrain-heal.sh` (outside lane) — I only confirmed the patched symbols exist at `src/cores/…` paths in `~/.bun/install/global/node_modules/gbrain` |
| Chunk-level proof that *new* writes stamp the signature correctly | already true at rest (0/24,430 mismatches, 0/80,053 non-conforming chunks); a future drift would only be visible post-hoc |
