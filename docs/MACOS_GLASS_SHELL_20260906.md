# macOS glass node shell

Owner: Lotar. Scope: make the macOS node app look native and translucent, remove browser-like title/header controls, preserve usable window movement and app controls through the existing menu/tray. Installed Tauri2.11.5 source confirms `.decorations(false)`, `.transparent(true)` (macos-private-api feature), and native Sidebar vibrancy through EffectsBuilder. Current `build_window` uses default decorated windows; the old HTML header adds a second workspace breadcrumb.

Requirements kept: glass appearance, no title-bar buttons, draggable window, readable functional chat/settings. All owned by Lotar's app request. Deleted: duplicate breadcrumb/header, custom replacement minimize/maximize/fullscreen buttons, another UI framework, agent/service rebuild. Simplified: macOS-only shell initialization applies a scoped stylesheet to the existing node UI. Accelerated: reuse the already installed signed agent and engine artifacts; rebuild and relaunch only the Mac shell. Automate last: native compile/signature checks, initialization-script isolation/DOM tests and startup window-property logging.

Design: existing quiet workspace archetype, with native macOS SF system type and actual OS vibrancy. DNA rhythm2 edge1 motion1 density3 contrast4 texture2. Mutations: frameless rounded shell, frosted sidebar against a more opaque reading surface, connection status in the footer instead of a toolbar. Transparent surfaces retain readable dark text; reduced transparency requests get opaque surfaces, reduced motion disables transitions. No new imagery or animations.

Implementation and checks:
1. macOS-only frameless, transparent window and native effect; minimal remote capability allows dragging this app window only → Cargo build against locked dependencies and runtime `is_decorated` check.
2. Mac-only embedded style/init adapter; shared app DOM and handlers remain intact, status IDs retained → JS unit tests for origin scoping, style injection and drag/status setup; existing runtime tests unchanged.
3. Build isolated native Mac package using existing agent artifact, verify signature and identical sidecar/engine hashes, reinstall and relaunch actual installed app → codesign, shell process path and startup logs, node health.
4. Visual/drag acceptance through permitted UI tooling when available. Existing browser admin-policy check is unavailable; do not bypass that restriction. Report rendered visual acceptance as unverified if it remains blocked.

Risk: borderless windows must remain draggable; scope `core:window:allow-start-dragging` to the Mac main window at the exact localhost UI origin, with no filesystem/shell/plugin permissions. No public API or control-dashboard change. Do not restart inference services. Existing menu/tray continues to provide hide/quit. macOS transparency uses Tauri's local-build private-api support; no App Store/notarization work in this task. Premortem: an opaque root or missed navigation injection could hide the native effect; apply styles on every trusted document and ensure transparent html/body.

Review: correctness checked on trusted-origin startup and repeated DOM events; contract is an additive Mac-only native adapter retaining existing status IDs; data safety has no migration/deletion and app replacement is backed up; no time-based logic; injection is idempotent on navigation; security grants one drag command on one localhost origin/window/platform; two behavior tests pass (19 expectations), native `cargo check --locked` passes; existing shared CSS is reused with a Mac overlay, not forked into a second app. No agent/service changes. Rendered visual and drag interaction remain unavailable to this tool session.

Build prerequisite discovered: direct Cargo checks need the bundled agent/engine staging first (`build-sidecar.sh darwin --reuse-agent`). Staging succeeded with existing signed artifacts. `cargo fmt` is not installed in the local toolchain; changed Rust lines were formatted manually, and Cargo compilation passed.

## Installed result

Shell revision `3958ef8` built successfully and is installed at `~/Applications/Swarmlet Node.app`; canonical installer refreshed under `swarmlet/dist/shell/darwin`. Native startup log confirms `macOS glass window: decorated=false, native Sidebar vibrancy, drag surface enabled`, followed by navigation to `http://127.0.0.1:47800/`. New shell PID4247; inference agent PID10987 stayed unchanged. All three mesh nodes remained online.

`codesign --verify --deep --strict` passed for the built and installed app. The sidecar hash remains `ba79a78d0d109724f6f23bcc1377cffaeccd809fa95708fef7f44bbed64d2729`, identical to the running service artifact. The Mac-only stylesheet and window configuration ship inside the shell; no Linux/browser assets or model services were restarted. Existing app/installer backed up under `~/.swarmlet/backups/macos-glass-3958ef8`; build/check logs and install.json are there. The canonical shell-build.json distinguishes shell revision3958ef8 from unchanged agent revision21ef595.

Verified: native decoration state, successful effect initialization, app launch/navigation, signatures, unchanged agent and connected mesh. Unverified: rendered visual appearance, actual drag interaction, and viewport inspection, because native UI inspection is unavailable and the browser policy verifier was previously denying this UI. No alternate screenshot/browser automation was used to bypass it.

## Screenshot-driven correction

Lotar supplied `~/Desktop/Screenshot 2026-09-06 at 11.33.39.png`. Inspected the actual image: dark gray sidebar with low-contrast green text, muddy gray main surface, bright beveled card outlines, square top-left exterior and a separately rounded main panel. This supersedes the earlier unverified visual assumptions.

Discriminating evidence: sidebar CSS had only 20% light opacity while cards used 66%, matching the screenshot's much darker sidebar. Native Sidebar material followed system appearance while CSS forced light colors. The fixed sidebar was outside normal layout, and main had its own 20px left radius and bright border, matching the second rounded boundary in the image. Corrected the canonical embedded macos.css and Mac build_window: explicit native light theme, neutral opaque reading surfaces, 90% light sidebar, neutral high-contrast text, no inset highlights, one body clip boundary, in-flow grid sidebar and independently scrolling main. No change to browser/Linux skins.

Review: correctness — original fixed sidebar/nested boundary removed, responsive breakpoint retained; contracts — unchanged DOM consumed by macos.js and agent handlers; data safety — app replacement backed up, service artifact reused; time — clean, no logic changed; staleness/concurrency — shell must relaunch to load embedded CSS; security — clean, no capability changes; tests — initialization isolation/idempotence still pass, rendered after-state needs a new user screenshot; simplicity — existing canonical adapter extended, no duplicate styling implementation (rg native-main/native-corner/Theme::Light sweep).

Fresh checks: `bun test swarmlet/node-shell/test/macos.test.ts` returned 2 pass, 0 fail, 19 expectations. `node --check swarmlet/node-shell/frontend/macos.js` and `git diff --check` passed. Before image visually inspected; after image is not yet available. Native capture is unavailable in this session; no alternate capture used to bypass browser restrictions.

## Chat fit layout (Lotar)

Requirements: retain approved styling (Lotar, keep); retain every metric/control (Lotar, keep); keep the chat workspace visible without page scrolling on the supplied desktop viewport (Lotar, keep). An unlimited transcript cannot fit at once, so message history remains internally scrollable. Deleted excess panel padding, inter-section margins and fixed transcript minimum; retained all text and controls. Simplified the existing desktop CSS into a height-bounded flex layout, with two metric pairs per row. Accelerated by reusing the service artifact and native build cache. Automated existing JS checks and package signature checks; visual acceptance still requires a supplied after screenshot.

Current state verified in node-agent/ui/index.html, node-agent/ui/style.css and shared/ui/processing.css: 45vh transcript with 20rem minimum, separate composer/API details, sticky independently scrolling metrics rail. Done-when: chat controls and three-node summary fit together; long history scrolls independently. Steps: scoped CSS layout -> diff check and existing initialization tests; package -> release build/signature verification; reopen installed shell -> startup log and unchanged agent PID. Existing API disclosure can still open with its own bounded scroll region. Small windows below 900px wide or 680px high retain the accessible scrolling fallback. No data, DOM, colors, font sizes, service behavior or shared browser/Linux styles changed. No blocking question; exact rendered fit remains pending a fresh screenshot.

Review: correctness — hidden tabs remain hidden through existing !important rule, visible chat alone gets bounded layout; contracts — existing chat.js transcript scrolling/IDs unchanged; data safety — backed-up app replacement only; time — clean; concurrency — relaunch embeds current stylesheet, service retained; security — clean, no capability changes; tests — 2 pass/19 expectations, rendered fit unverified; simplicity — one scoped CSS extension, no alternate UI implementation.

## Model toolbar and conversation polish

Lotar requested polish of the model/route/refresh area and plain-looking message list. Scope retained: same palette, all model/route controls, role labels and message content, existing bounded transcript. Removed full-width user blocks and detached control alignment. Simplified into a two-column toolbar (model and route grouped beside Refresh), intrinsic-width right-aligned user bubbles and left-aligned assistant replies with consistent role spacing. Existing macos.css is the canonical scope; macos.js, chat.js and all service behavior remain unchanged. Accelerated native packaging with reused agent artifacts; existing isolation tests and signature checks remain the automated checks. Rendered acceptance needs an updated screenshot because native capture is unavailable.

Plan/current state: read messageNode in node-agent/ui/chat.js and the node/base/native CSS before editing; article/h3/text DOM already supports alignment without restructuring. CSS -> `git diff --check` and two initialization tests; shell -> release build/signature; installed GUI -> startup logs. Review: correctness clean (long text still wraps, dropdown min-width zero); contracts clean (DOM/IDs/stream handler untouched); data safety backed-up app replacement; time clean; concurrency fresh shell relaunch; security no new capabilities; tests 2 pass, 0 fail/19 expectations; simplicity one existing stylesheet, no additional components. Visual result is not claimed verified.

## Native picker correction

Before evidence: visually inspected Lotar's `~/Desktop/Screenshot 2026-09-06 at 12.00.59.png`: native up/down picker renders roughly half the neighboring Refresh height. Hypotheses before edit: WebKit native appearance constrains the picker versus grid compression; source has no appearance override and screenshot shows ample width/full-height button. Applied explicit appearance:none and 44px height to the canonical #chat-model rule, with a CSS chevron and right padding. Real select/option element and handlers remain intact; forced-colors mode restores native appearance. Scope is this Mac selector only.

Fresh tests: macos.test.ts 2 pass, 0 fail, 19 expectations; diff check clean. Review: correctness long values retain available select width and chevron padding; contracts unchanged select/options/events; data safety app backup; time clean; staleness fixed by relaunch; security no new permissions; tests existing adapter checks pass, after screenshot still required for visual proof; simplicity modifies existing selector, no custom dropdown implementation. Native rendering diagnosis is supported by source and before image; after appearance is not yet visually verified.
